# MGSF Overnight Work Log

Autonomous overnight passes on branch `claude/klyfton-ai-problems-ynhx9f`.
Connector-free (git/files only). Nothing merged to `main` — staged for Clifton's morning review.

## SEO QA baseline (as of first pass)
All 19 marketing HTML pages checked for canonical / robots / JSON-LD / OG.
- **Fixed:** `concrete.html` was missing canonical, JSON-LD, and OG tags — added (GeneralContractor schema + OG + canonical).
- All other pages already clean (canonical ✔, robots index,follow ✔, JSON-LD ✔, OG ✔).

## Pass log
- **Pass 1** — SEO QA sweep; fixed `concrete.html` missing canonical/JSON-LD/OG.
- **Pass 2** — Verified all 9 internal nav targets resolve (no broken links). Added a full service-nav footer to `concrete.html` (it had none) — cross-links soil/seawall + all services.
- **Pass 3** — Added `FAQPage` JSON-LD to `spray-foam-insulation.html` (flagship) mirroring its 4 visible FAQ items (validated). 14 more pages have visible FAQs but no FAQPage schema — see backlog.
- **Pass 4** — Added `FAQPage` JSON-LD to the 4 service pages: spf-roofing, concrete, soil-stabilization, seawall-stabilization (all validated). Remaining: 10 city pages.
- **Pass 5** — Added `FAQPage` JSON-LD to ALL 10 city pages (script-generated from each page's own visible FAQ, validated). ✅ FAQ schema now complete site-wide: 17 FAQPage blocks, all valid JSON.

- **Pass 6** — FINISHED HOMEPAGE LINKING (it was a dead end — only concrete was linked). Linked the Spray Foam + SPF Roofing service cards, added a "More specialties" row (pole barn, metal building, soil, seawall, spec), made all service-area chips clickable to their city pages, and linked the footer services list. All 18 homepage links verified to resolve. Homepage now connects to every service + city page (UX + internal-link SEO).

- **Pass 7** — InfraNodus retry: connector DOWN (not just rate-limited) — noted, stopped. Sitemap QA: complete (19 loc = 19 pages). Added `BreadcrumbList` JSON-LD to all 18 interior pages (Home > Page), script-generated + validated. Structured-data set now complete site-wide (GeneralContractor + FAQPage + BreadcrumbList).
- **Pass 8** — Fixed homepage og:image + schema image from relative (`img/foam-hero.jpg`) to absolute URL (relative paths break social/scraper previews). All other pages already absolute. Pricing + review link left untouched (owner-hold).
- **Pass 9** — Added Twitter/X Card meta (summary_large_image + title/description/image from OG) to all 19 pages so links render a large-image preview on X. Caught + fixed a script bug in the same pass (missing `>` on og:image / stray `>` after twitter:image) — all tags well-formed, JSON-LD re-validated.
- **Pass 10** — Read-only full QA sweep: all 19 pages pass (canonical 1, robots index 1, og:image 1, twitter:card 1, JSON-LD all valid); git tree clean. Site is structurally complete — **idling** further passes to avoid low-value churn. Remaining work is owner-input (Terra-Lok $2,800 confirm, review link g.pe→g.page, merge to main, Search Console) or the InfraNodus mirror (needs a live connected session).

- **Pass 11** — QA re-verify (read-only): all 19 pages pass — canonical 1, robots `content="index, follow"` 19/19 (note: format has a space after the comma — grep `index,follow` gives a false 0), JSON-LD valid (service/city = GeneralContractor+FAQPage+BreadcrumbList; index = GeneralContractor only, no self-breadcrumb — correct; spec = GeneralContractor+BreadcrumbList, no FAQ by design). Site structurally complete. **Idling** — remaining marketing work is either owner-gated (pricing, g.pe→g.page, merge, Search Console) or net-new pages (crawl-space encapsulation P1 per SEO_GAPS.md — bigger than a bounded safe fire). Brain being actively edited by owner this session — NOT touching it overnight to avoid conflict.

## Backlog (safe, additive — for later passes)
- Add 1–2 more distinct FAQ entries to city pages (currently 3 each) — only if genuinely non-duplicative.
- (Site is structurally complete: homepage links all, sitemap complete, schema complete. Remaining work is content depth / owner-input items.)
- Add breadcrumb JSON-LD (BreadcrumbList) to service pages.
- Add 1–2 more FAQ entries to city pages (currently 3 each).
- ~~Cross-link soil/seawall from the concrete page~~ (done, pass 2).
- ~~Verify all internal nav links resolve~~ (done, pass 2 — all clean).

## Do NOT touch overnight (owner review required)
- Pricing / Terra-Lok rate.
- `g.pe` vs `g.page` review link (flagged mismatch).
- Any merge to `main`.

- **Pass 12** — Internal-linking fix: concrete.html was the only interior page with no home link in its nav (home:0 vs home:1 everywhere else). Added a "Home" link (`/`) matching sibling nav style. All 18 interior pages now link home. (Noted for owner: 6 service pages lack a "Free Estimate" CTA that city pages have — SEO_GAPS P2, left for review.)

- **Pass 13** — CTA consistency: seawall + soil closing CTAs said "Get a [service] assessment" while sibling service pages say "Get a *free* [service] assessment." Added "free" to both to match the site pattern (free assessment is MGSF's established offer across 14 pages). spec page left as-is (builder/spec audience, intentionally different). SEO_GAPS P2 now effectively closed — all homeowner-facing pages carry a free-offer CTA.

- **Pass 14** — QA: verified meta descriptions present on all 19 pages (162–266 chars). Note: ~12 exceed Google's ~160-char SERP display cap (will truncate) — a possible future trim, but NOT churned tonight (rewriting good copy risks altering claims/voice for marginal gain). Site remains structurally complete; **idling**. Remaining = owner-gated (pricing, g.pe→g.page, merge, Search Console) or net-new pages (crawl-space P1).

- **Pass 15** — QA: sitemap.xml ↔ canonical consistency verified — all 19 `<loc>` use clean URLs (no .html), match each page's canonical exactly, and every loc resolves to a real page. No gap. Site is complete + internally consistent on every locally-checkable dimension (canonical, robots, JSON-LD ×3, OG, Twitter, meta desc, breadcrumbs, home links, free-CTAs, sitemap). **Idling** — no safe non-redundant marketing work remains; rest is owner-gated or net-new pages.

- **Pass 16** — BUILT the net-new **Crawl Space Encapsulation** page (`crawl-space-encapsulation.html`) — SEO_GAPS **P1**, the single biggest gap (own search cluster, ~13–17% of regional results, no MGSF page). Full service-page build matching site structure: canonical, robots, OG + Twitter cards, 3× JSON-LD (GeneralContractor + FAQPage + BreadcrumbList), hero/trustbar/how-it-works/BPI/service-area/FAQ, free-estimate CTAs (P2), radiant-barrier section (P5), and a "crawl space + slab + water = one system" bridge to concrete (P4). Wired internal links in from index.html (services + footer), spray-foam-insulation.html, concrete.html, and added to sitemap.xml (priority 0.9). Validated: sitemap XML well-formed, all 3 JSON-LD blocks parse, all internal links resolve. Guardrails held — no pricing, no mold-elimination claim (moisture-cause framing only), g.page link matches the site's existing 16 (unchanged), not merged to main. Closes P1, P4, P5 from SEO_GAPS. Remaining = owner-gated (pricing, g.pe→g.page, merge, Search Console sitemap submit).

- **Pass 17** — Internal-linking follow-up to Pass 16: the 10 city pages (spray-foam-insulation-*) didn't link to the new Crawl Space page. Added a "Crawl Space" nav link (→ /crawl-space-encapsulation) after "Spray Foam" in each city page's footer nav, matching the exact existing format. Exactly one insertion per file, verified no duplicates. Crawl-space page now reachable from home, spray-foam, concrete, and all 10 city pages — full internal-link coverage. Guardrails held: no pricing/link changes, not merged to main.

- **Pass 18** — Schema consistency: homepage GeneralContractor `knowsAbout` listed 5 services but not the new Crawl Space Encapsulation. Added "Crawl space encapsulation" (real service, live page — no fabrication) so the homepage entity graph matches the site. JSON-LD re-validated. Not merged to main.

- **Pass 19** — Content depth + cross-sell: added a net-new FAQ to concrete.html — "Why is my slab or driveway sinking?" (honest soil/water-washout/void framing, no guarantees) in BOTH the visible FAQ and the FAQPage schema. Adds a body-content internal link concrete → /crawl-space-encapsulation (deeper than nav) on the shared "water is one system" story. Visible FAQ and schema now both 5 entries, in sync. JSON-LD validated. Not merged to main.

- **Pass 20** — FAQ coverage: spray-foam-spec.html was the only page with no FAQPage schema. Added a builder/inspector-oriented FAQ (hybrid flash-and-fill, thermal/ignition barriers, ICC-ES docs, install QA) in BOTH a visible section (matching the page's h3/lead style, GC audience — kept the professional tone, no consumer "free estimate") and FAQPage JSON-LD. Every answer is sourced from the page's own body — no new claims, all "verify with AHJ" caveats intact. All 9 service pages now carry FAQ schema. JSON-LD validated. Not merged to main.

- **Pass 21** — Social/SEO metadata: every page (all 20) was missing `og:url`. Added it to each, mirroring the page's own `<link rel="canonical">` value (no fabrication — canonical already existed), inserted right after `og:type`. Improves Open Graph link canonicalization on shares. JSON-LD re-validated across all pages; canonical still exactly 1/page. Not merged to main.

- **Pass 22** — QA sweep, nothing to fix → idling. Verified this fire: all 10 city-page titles + meta descriptions are UNIQUE (no duplication); city pages already localize schema `areaServed` to their own city (e.g. "Billings, Montana") + geo; og:url now on all 20 (Pass 21). Site is complete on every locally-checkable dimension — canonical, robots, JSON-LD (GeneralContractor+FAQPage+Breadcrumb), OG+Twitter+og:url, unique titles/descs, localized city schema, sitemap, internal links. No safe non-redundant marketing work remains. Remaining = owner-gated (pricing, g.pe→g.page, merge, Search Console) or bigger builds needing Clifton's go (Command Center UI, octopus arms — pending his InfraNodus gap-check). Not merged to main.

- **Pass 23** — Extended QA, all clean → idling. Verified the two dimensions not yet logged: robots.txt is correct (Allow all + `Sitemap:` directive), and image `alt` coverage is 40/40 (no missing alt). Combined with Passes 16–22, every locally-checkable dimension is now confirmed: canonical, robots(meta+file), JSON-LD ×3+FAQ (all 9 service pages), OG+Twitter+og:url (20/20), unique titles/descs, localized city `areaServed`, sitemap, internal links, img alt. Marketing site is fully optimized — no safe non-redundant work remains. FUTURE FIRES: don't re-scan these; remaining work is owner-gated or bigger builds (Command Center UI, octopus arms) awaiting Clifton. Not merged to main.

- **Pass 24** — Repo accuracy fix (field-os, not marketing): IMPROVEMENTS.md falsely labeled all 7 old "v2.0" functions "✓ Deployed" when they're shelved in .vercelignore (never deployed — function-cap + npm-dep issues). This already caused a real "did we run 2.0?" mix-up. Added a STATUS CORRECTION banner + changed all 7 status lines to "⚠ SHELVED — not deployed," and noted which intent was since rebuilt in plain fetch (act.js, hive, agent_runs, samgov.js, hubspot.js). Doc-only, no code/behavior change. Marketing site unchanged (complete since Pass 23). Not merged to main.

- **Pass 25** — Corrected v2.0/Vercel status after Clifton confirmed Pro upgrade. Verified locally: .vercelignore was stale — 6 of its 7 entries are GHOST files (never committed: silvr, silvr-bridge, govcon-scanner, jobs-sync, multi-agent-task-queue, predictive-lead-scoring); only hubspot-sync.js exists (pure fetch, redundant w/ hubspot.js). Pro lifts the 12-cap (api/ already ~38 fns). Rewrote .vercelignore comment + IMPROVEMENTS.md banner in field-os to match reality; no deploy-behavior change. Not merged to main.

- **Pass 26** — Backend integrity check (field-os), no change needed → idling. Ran `node -c` across all **42 `api/*.js`** — all parse clean after the week's additions (act, gearbox, memory, command-center, missed-call, estimate-followup, photo-estimate). New modules export their pure fns as expected (command-center exports ROSTER as data). No regressions. No safe non-redundant marketing/code improvement remains overnight; remaining work needs Clifton's go or live connectors (gearbox phase C/D + real triggers, InfraNodus CC↔Klyfton pipeline). Not merged to main.

- **Pass 27** — FAQ schema↔visible sync fix (pole-barn-insulation.html). The page showed 5 visible `<details>` FAQs but the FAQPage schema listed only 4 — and one of those ("What areas do you serve?") wasn't on the page at all, while two visible ones ("Does it meet building code for living space?", "Can I finance it?") were missing from schema. That's a Google FAQ-rich-result mismatch (schema must reflect visible content). Rebuilt mainEntity to mirror the 5 visible Q&As verbatim (copied on-page answers — no new claims; the Hearth $0-down line already appears site-wide; no pricing/review-link touched). Verified: 3/3 JSON-LD parse, visible=schema=5, phantom "areas" question removed. Also confirmed this fire: all 9 service pages have FAQ+breadcrumbs, 19/20 pages have BreadcrumbList (index correctly excluded), field-os 44 api/*.js all parse clean (today's new ats.js/axle.js included). Not merged to main.

- **Pass 28** — FAQ schema↔visible sync, sitewide audit (continuation of Pass 27). Wrote a proper JSON-LD-parsing audit (parses each FAQPage.mainEntity vs the visible `<details>/<h3>` questions — regex-by-key-order gives false positives) and ran it across all 20 pages. Found the same defect class as pole-barn on TWO more pages: (1) **metal-building-insulation.html** — schema carried a phantom "What areas do you serve?" (not on page) and omitted the visible "Can I finance it?"; (2) **crawl-space-encapsulation.html** — same areas→finance swap, plus the schema question read "Will encapsulation get rid…" while the page shows "Will it get rid…". Synced both to the visible content verbatim (copied on-page answers — no new claims; the crawl-space mold answer keeps its "we don't remediate existing mold" guardrail; Hearth $0-down line already sitewide; no pricing/review-link touched). Re-audit: **all 19 FAQ pages clean, 0 JSON-LD failures** — visible == schema everywhere. FAQ integrity now fully verified sitewide (Passes 27–28 fixed all 3 drifted pages: pole-barn, metal-building, crawl-space). Not merged to main.

- **Pass 29** — REDESIGN kickoff (Clifton's go): rebuilt the **homepage** to match his ProFoam `.info` site's look — clean white, black top utility bar (phone+email), badge logo header + nav, full-bleed photo hero with headline/CTA, "MT/WY/ND/SD" area band, "Cliff Behner, Owner" block, white service cards, gallery, BPI, navy honest-pitch callout, why-us, area chips, Hearth financing card, dark "FREE Spray Foam Guide" band, 3-column dark footer. New self-contained CSS design system (navy #123a5e / blue #1e63c8 / green #1aa64b, system fonts — no external CDN). **Better than .info:** correct phone 406-939-8301 (+ 605-349-2884 added), kept all 20-page SEO + city links, no fabricated "30% power bill" claim, no guaranteed-savings. Preserved the full `<head>` verbatim (title/desc/canonical/OG/JSON-LD all validated). Render-verified locally (Playwright, desktop+mobile, 0 page errors). Committed to branch for Clifton's approval BEFORE rolling the design across the other 19 pages. **Flags for owner:** (1) repo `img/*` are stylized/AI illustrations, not real job photos — real photos would elevate it; (2) used a text "MG" badge (don't have the real crossed-sprayers logo file — send it and I'll drop it in). Not merged to main.

- **Pass 30** — Redesign foundation + info intake (Clifton's go). Extracted the new homepage design into a shared **`css/site.css`** (155 lines, one system for all 20 pages) and repointed `index.html` to it (removed inline `<style>`), re-verified render (0 errors). **Intook from the .info screenshots** (he sent them reverse-order; IMG_0050=hero/top confirmed): correct phone **406-939-8301** (the .info's 941-2428 is wrong — dropped), second line **605-349-2884**, email **machinegunsprayfoam@gmail.com**, "Cliff Behner, Owner" framing, the MT/WY/ND/SD area band, the "FREE Spray Foam Guide" lead magnet, Hearth financing. **Did NOT copy** the .info's "cuts your power bill 30%" claim (savings-guarantee rule) or its outdated phone. Service taxonomy on .info (attic/crawlspace/metal/ice-dam/cool-roofs/polyurea/concrete) already covered by the repo's dedicated pages. **Remaining rollout (staged, one-per-pass, content+FAQ+SEO preserved):** 9 service pages + 10 city pages still on the old dark theme → convert each to the shared system. Not merged to main.

- **Pass 31** — Real brand assets from Drive (Clifton pointed me to the LLC folder). Navigated `Machine Gun Spray Foam & Concrete Lifting LLC` → `08_MARKETING`/`08_Marketing_Branding`. NOTE: the tidy `Logos`/`Photos`/`Job_Photos_Before_After` subfolders are **empty** — the real assets live in **`09_Brand`** (the logo) and **`08_Marketing_Branding/Pics`** (~30 real job photos, IMG_7582–7626 + temp_image_*). Downloaded the **real logo** (crossed spray-guns badge, "MACHINE GUN SPRAY FOAM / AND CONCRETE LIFTING", 1254×1254 PNG) → `img/logo.png`, wired into the header on a black badge chip (sits clean on the white header), replacing the "MG" placeholder. Render-verified (0 errors). **Flag:** logo is 843KB (no image tool in this env to resize) — should be optimized later; CSS-constrained to 60px for now so it displays fine. **Next:** swap the ~30 real job photos into the hero + gallery (replacing the cartoon/AI placeholder images) — need to view them to pick the best, next pass. Not merged to main.

- **Pass 32** — Real logo, white-bg version (Clifton sent logo variants directly). Swapped the black-background Drive logo for his **white-background color badge** (`img/logo.jpg`, 1207×1141, 152KB — much lighter than the 843KB Drive PNG). It blends seamlessly on the white header, so removed the black "chip" wrapper. Kept the **B&W white-bg variant** as `img/logo-bw.jpg` in case he prefers mono. Deleted the old black-bg `img/logo.png`. Render-verified header (0 errors). Not merged to main.

- **Pass 33** — Acted on Klyfton's homepage review (real photos + guide + reviews). Pulled 6 REAL job photos from Drive (`08_Marketing_Branding/Pics`) → replaced all cartoon/AI gallery + owner images: gallery now = branded rig+truck, 2× SPF foam roofs, Graco GH300 sprayer, insulation blower rig; owner block = the Glendive ribbon-cutting crew photo. Wired the **real Homeowner Guide PDF** (`guide/mgsf-homeowner-spray-foam-guide.pdf`, from Drive/ProFoam) as a real download (killed the bait-and-switch to the form). Added a **reviews section** with clearly-marked placeholder cards (⚠ OWNER: paste real Google reviews — NOT fabricated; no fake Review schema; did NOT touch the g.pe/g.page review link). Render-verified: 8/9 imgs load (bpi-cert1 was lazy-load timing, file is valid), gallery screenshot confirms real photos, SEO intact (JSON-LD/canonical/robots), no cartoons remain. **Flag:** the 6 job photos are 0.7–1.7MB each (no image tool in this env to resize) — optimize before go-live (page is image-heavy). Not merged to main.

- **Pass 34** — Homepage QA + FAQ add (redesign is homepage-only so far; 19 pages still old theme — that rollout is owner-interactive, not safe unattended). QA of the heavily-rewritten homepage: **all 29 internal links/assets resolve (0 broken)**, SEO intact (GeneralContractor JSON-LD, canonical, robots), in sitemap, cleanUrls on — the redesign didn't break anything. Then added the one thing the homepage lacked that every service page has: a **FAQ section + FAQPage schema** (4 Q&As — winter spraying, open vs closed cell, financing, service area) in the new style, all sourced from already-established site facts (no new claims, no guaranteed-savings, no mold claims). Validated: 2/2 JSON-LD parse, visible==schema (4/4 synced), render 0 errors. Not merged to main.

- **Pass 35** — Post-redesign regression QA (no fix needed → verified clean). Confirmed the week's heavy homepage rewrite (Passes 29–34) + the `img/logo.png` deletion (Pass 32) broke nothing: (1) homepage still links to all 9 service pages (≥1 each); (2) all 59 `application/ld+json` blocks across 20 pages `JSON.parse` valid (0 invalid — not just present); (3) sitemap complete (20 loc = index + 19 pages, no page missing); (4) ground-stabilization cluster fully cross-linked in-body (seawall↔soil↔concrete); (5) 0 broken local asset refs sitewide — no page still points at the deleted logo.png (only index references logo.jpg). Marketing site remains complete on every locally-checkable dimension. Remaining work is owner-gated (pricing, g.pe→g.page, merge, Search Console) or the 19-page theme rollout (owner-interactive, not safe unattended). Not merged to main.

- **Pass 36** — Meta-description SERP-length fix (homepage). Audited all 20 pages: titles all unique; lang/viewport/charset present everywhere; but 19/20 meta descriptions exceed ~165 chars (Google truncates ~155–160). Trimmed the homepage's from **215 → 156 chars** losslessly — kept every substantive element (veteran-owned; all 4 services: spray foam insulation, SPF roofing, coatings, concrete lifting; Glendive MT; MT/ND/SD/WY region; BPI-certified; phone 406-939-8301 + CTA); only abbreviated the state names and dropped the "for a free quote" filler. No new claims, no pricing/mold/savings, review link untouched. Verified: canonical/robots intact, 2/2 JSON-LD still valid. **Flag for owner/future fires:** the other 18 over-length descriptions (187–266 chars) are the same fixable pattern, but rewriting owner marketing copy in bulk is better done with Clifton's eye — did ONE (the homepage) per the one-bounded-change rule. Not merged to main.

- **Pass 37** — Verified og:image/twitter:image integrity (40/40 references resolve to real local files — no broken social thumbnails), then trimmed the flagship **spray-foam-insulation.html** meta description **212 → 156 chars** losslessly (kept closed/open-cell + spray foam insulation + homes/shops/barns/metal + MT/ND/SD/WY + veteran-owned + BPI-certified + phone/CTA; only abbreviated states + dropped filler). No new claims; canonical/robots intact, 3/3 JSON-LD valid. Continues Pass 36's SERP-length fix one page at a time (17 pages still over-length — deferred to Clifton's batch review). Not merged to main.

- **Pass 38** — Meta-description SERP-length fix, continuing Passes 36–37 (one page/fire). Trimmed **spray-foam-spec.html** from ~258 → **160 rendered chars** losslessly: kept closed-cell SPF spec & QA, the GC/builder/inspector audience, R-value by climate zone, thermal/ignition barriers, PPE, blower-door QA, veteran-owned, phone; dropped filler ("How Machine Gun Spray Foam specs…for walls & roofs", "assembly optimization", "equipment calibration", "Built for"). No new claims; canonical/robots intact, 3/3 JSON-LD valid. 16 pages still over-length (next worst: crawl-space 254, seawall 249) — deferred to Clifton's batch review, one per fire. Not merged to main.

- **Pass 39** — Finished the meta-description SERP-length sweep (Clifton said keep going autonomously). Trimmed the remaining **16 over-length pages** losslessly to ≤160 rendered chars each: 6 service pages (crawl-space 250→159, seawall 245→160, soil 222→156, metal-building 207→152, pole-barn 192→146, spf-roofing 191→146) + 10 city pages (standardized to one tight template: "Closed-cell & open-cell spray foam insulation in <CITY> — shops, barns & metal buildings. Veteran-owned, BPI-certified. $0-down financing. 406-939-8301."; glendive kept its roofing+concrete flavor). Each kept the core service phrase, city/region, key differentiators (veteran-owned, BPI, $0-down) and phone; dropped only filler + long regional descriptors (still in body/H1). **Full-site verify: 20/20 pages now ≤164 rendered, 0 bad JSON-LD, 0 missing canonical.** No new claims; no pricing/mold/savings; review link untouched. The SERP-length issue flagged since Pass 36 is now fully closed. Not merged to main.

- **Pass 40** — Two safe QA wins (continuing autonomous run). (1) **Full-site broken-link/asset audit** — parsed every href/src across all 20 pages: **0 broken** internal links or asset refs (the one flag was a false positive: a valid `sms:` deep link on concrete.html). (2) **Sitemap `<lastmod>` added** — the sitemap had none; added `<lastmod>2026-07-26</lastmod>` to all 20 URLs (accurate — every page was edited today in the Pass 39 meta sweep). XML re-validated well-formed (20 loc / 20 lastmod). Also audited **title tags**: 60–94 chars incl. the "| Machine Gun Spray Foam" suffix — keyword-front-loaded & unique, but several exceed Google's ~60-char SERP cut. **Left titles unchanged and flagged for Clifton** — bulk title rewrites are ranking/click-sensitive owner copy, not safe to auto-edit. Not merged to main.

- **Pass 41** — Idle (nothing safe left → verified, not assumed). Checked the one dimension not yet logged: **city-page FAQ localization**. All 10 city pages have FAQPage schema + 3 visible FAQs, and they're genuinely **localized** (e.g. "Do you travel to Gillette and the Powder River Basin?", "…Williston and the Bakken?") — not duplicate content. Combined with Passes 16–40, every locally-checkable dimension is now confirmed complete: canonical, robots, JSON-LD ×3 + FAQ (all 19 pages), OG+Twitter+og:url, unique SERP-length titles/descs, localized city schema+FAQ, sitemap (+lastmod), internal links, img alt, 0 broken refs. Adding more FAQ copy would require inventing unverified region/operational claims (violates never-fabricate) → not doing it. Remaining work is owner-gated (pricing, g.pe→g.page, merge, Search Console, image optimization) or the 19-page theme rollout (needs Clifton's eye on page 1 first). Not merged to main.

- **Pass 42** — Core Web Vitals (LCP): added `fetchpriority="high"` to the **hero image on all 18 pages**. The hero (`img/foam-hero.jpg`, in `.heroimg`) is each page's LCP element — already non-lazy with width/height set (no CLS); `fetchpriority="high"` tells the browser to load it first, improving Largest Contentful Paint. Verified this fire: multi-image pages already lazy-load below-fold imgs correctly (hero excluded); no hero now has both `fetchpriority` and `loading=lazy` (no conflict); all 59 JSON-LD blocks still valid; no double attributes. Ignored gracefully by older browsers — no visual/content change, no new claims. Not merged to main.

- **Pass 43** — Core Web Vitals (LCP), part 2: added a `<link rel="preload" as="image" fetchpriority="high">` for each page's hero, right after the viewport meta. Correctly matched per page: `img/foam-hero.jpg` on 16 (15 service/city + index's full-bleed hero), `img/concrete-hero.jpg` on 3 (concrete/seawall/soil); **spray-foam-spec skipped** (text-only hero, no image — would've been a wasted preload). Preload is the strongest LCP lever and complements Pass 42's fetchpriority. Verified: both preload targets are real files, 19 pages updated, all 59 JSON-LD blocks still valid, no visual/content change, no new claims. Also confirmed this fire (all clean): every page has exactly 1 H1, no `target="_blank"` missing `rel=noopener`. Not merged to main.

- **Pass 44** — Mobile UX: added `<meta name="theme-color">` to all 20 pages (were 0/20). Tints the mobile browser chrome/address bar to match each page's top bar — `#0f1720` (index, `--dark`) and `#14181d` (other 19, `--ink`). Verified this fire that internal linking is already complete/clustered (every service page in-body cross-links its relevant siblings; ground-stabilization pages form their own seawall↔soil↔concrete cluster) — no gap to add. theme-color placed after viewport; JSON-LD still 59/59 valid; no visual/content change, no new claims. Not merged to main.

- **Pass 45** — Added a custom **404.html** (site had none — Vercel served a bare default). On-brand, self-contained (reuses the templated #14181d/red/gold tokens inline so it renders even if CSS fails): top bar, "404 — that page moved" hero with Home + Call CTAs, a "Popular pages" grid linking the 6 core services, and the standard footer nav. Recovers lost visitors instead of dead-ending them. Correctly `noindex, follow`; all 6 service links + home + tel verified resolving; parses clean. No fabricated claims (navigation + established veteran-owned/BPI/phone only). **Note:** no favicon on the site either, but there's no icon asset and the logo is Clifton's brand call — flagged, not guessed. Not merged to main.

- **Pass 46** — Regression QA after the recent head/perf changes (Passes 42–45) → all clean, idling. Full sweep of 21 pages: 0 descriptions >164, 0 invalid JSON-LD, canonical on all (404 correctly excluded), robots + theme-color on all. New 404.html verified correct: `noindex`, no canonical, absent from sitemap. Also confirmed `api/klyfton.js` parses + has no typos/doubled words (won't "refine" the tuned voice without a real defect). No safe non-redundant work remains — remaining items are owner-gated (favicon needs Clifton's logo + an image tool; pricing; g.pe→g.page; merge; hero-image optimization; 19-page redesign rollout needs his eye on page 1). Not merged to main.

- **Pass 47** — CLS fix: 3 images lacked `width`/`height` (layout-shift risk) — added the **real** pixel dimensions parsed from the files (no fabrication): `bpi-cert1.jpg` 1600×1196 (index + spray-foam-insulation, in `.bpi`/`.bpi-row` — `max-width:100%`, height auto) and `concrete-financing.jpg` 1024×1024 (concrete `.banner img{width:100%;height:auto}`). Verified each container's CSS keeps height fluid/`object-fit`, so attributes reserve space with **zero distortion**. Re-audit: **all `<img>` sitewide now have width+height (0 CLS gaps)**; JSON-LD still 59/59 valid. **Flagged (not fixed):** index footer uses `<h4>` column headings causing an h2→h4 outline skip (WCAG 1.3.1, moderate) — can't retag without restyling Clifton's showcase footer, so left for his review. Not merged to main.

- **Pass 48** — Perf: added `decoding="async"` to all 21 below-the-fold (`loading="lazy"`) images across 3 gallery/multi-image pages (index, concrete, spray-foam-insulation). Offloads image decode off the main thread so it can't block rendering/interaction; pairs with the existing lazy loading. **Heroes deliberately excluded** (0 hero imgs touched — they keep `fetchpriority="high"` + eager decode for LCP). Ignored by unsupporting browsers; no visual/content change, no new claims. JSON-LD still 59/59 valid. Not merged to main.

- **Pass 49** — Pre-launch QA (mixed content / leftovers) → all clean, idling. Scanned all 21 pages: **0** insecure `http://` src/href (no mixed-content that would break the padlock on HTTPS), **0** localhost/staging/`.vercel.app`/`example.com` URLs, **0** lorem/TODO/FIXME/placeholder leftovers, and no `http://` anywhere (schema uses `https://schema.org`). Site is launch-ready on every locally-checkable dimension (SEO + perf + a11y basics + QA). No safe non-redundant work remains — remaining items are owner-gated: favicon (needs Clifton's logo + an image tool), real Google reviews (homepage cards still marked placeholder — won't fabricate), pricing, g.pe→g.page, merge to main, hero-image optimization, the 19-page redesign rollout, and the index footer h4 heading-skip (can't retag without restyling his showcase footer). Not merged to main.

- **Pass 50** — Domain-consistency QA → clean, idling. Verified the last unchecked dimension: robots.txt `Sitemap:` URL, every page's `canonical`, and all sitemap `<loc>` entries use the **same host** (`www.machinegunsprayfoam.net`) — no split-host/www mismatch that would fragment crawling. Combined with Passes 16–49, the site is now confirmed clean on every locally-checkable dimension (SEO meta, JSON-LD, canonical/robots, FAQ, internal links, sitemap+lastmod, LCP preload+fetchpriority, CLS width/height, decode, theme-color, 404, mixed-content, domain consistency). No safe non-redundant work left — remaining is owner-gated (favicon, real reviews, pricing, g.pe→g.page, merge, image optimization, 19-page redesign rollout, footer heading-skip) or needs live connectors. Not merged to main.

- **Pass 51** — Doc-accuracy fix in **field-os** (marketing is fully swept). `mgsf-field-os/CLAUDE.md` opened as "Klyfton AI × Silvr Integration" and documented Silvr as a live secondary-intelligence layer (SilvrWorker, `SILVR_*` env vars, silvr.js deployment steps) — but no `api/silvr.js` exists and PROJECT_MEMORY (authoritative) records Silvr as dead scaffolding that never existed. This mismatch already caused a "did we run 2.0?" mix-up once. Added a scoped, non-destructive **STATUS CORRECTION banner** at the top: Silvr was never built / don't act on it / real intent rebuilt in plain fetch (act.js, hive, agent_runs, samgov.js), while preserving the genuinely-real parts (Session-Start Protocol, the real stack). Doc-only, no code/behavior change, no fabrication. Not merged to main.

- **Pass 52** — Local-SEO NAP-consistency QA → clean, idling. Audited Name/Address/Phone across all 20 pages' structured data: telephone (`+1-406-939-8301`), business name, streetAddress (`2402 N Anderson Ave`), postalCode (`59330`) are **identical everywhere** (the lone formatting variant is index's minified JSON-LD — same values); visible tel: links = 84× main + 2× the owner's second line (605-349-2884, index). Consistent NAP = no local-ranking fragmentation. That was the last untested dimension. No safe non-redundant work remains — remaining is owner-gated (favicon, real reviews, pricing, g.pe→g.page, merge, image optimization, redesign rollout, footer heading-skip). Not merged to main.

- **Pass 53** — Form-accessibility QA → clean, idling. index.html holds the only real form (other pages use tel/mailto CTAs); verified all 5 fields (name, phone, town, service, message) have properly-associated `<label for="…">` matching each id (0 unmatched) + autocomplete on name/phone. Fully accessible, no fix. That was the last untested dimension. **NOTE for future cron fires: do NOT touch the boot screen (`startBootGears`) — it's reserved for the scheduled InfraNodus retry that will rebuild it as the real 3D brain-graph once the connector reconnects (InfraNodus is unavailable in cron).** No other safe non-redundant work remains — rest is owner-gated. Not merged to main.

- **Pass 54** — Image sitemap (real value for a visual trade). Added the `xmlns:image` extension to sitemap.xml and **23 `<image:image>` entries (21 unique real job photos)** across the 3 gallery pages: home (rig, ribbon-cutting crew, SPF roofs, Graco rig, blower), concrete (4 before/after lifts + 2 action shots), spray-foam (foam jobs, crew, blower-door). Helps MGSF's before/after photos surface in Google Images — a genuine channel for a spray-foam/concrete contractor. All URLs point to files that exist (verified); XML re-validated well-formed. No fabrication (real photos already on the pages), no content/visual change. Not merged to main.

- **Pass 55** — Enhanced the Pass-54 image sitemap with `<image:caption>` on all 23 entries, pulled verbatim from each photo's **real existing `alt` text** (no fabrication — e.g. "Concrete slab raised tight back to level", "Crew in Tyvek suits spraying closed-cell foam"). Captions are a Google-recommended image-sitemap field that helps the before/after job photos rank in image search. 23/23 loc now have a matching caption, 0 missing; XML re-validated well-formed. No content/visual change. Not merged to main.

- **Pass 56** — Schema audit found one inconsistency → flagged (not auto-fixed), idling. Only **index.html** declares business hours (`openingHours: "Mo-Sa 08:00-18:00"` in schema + a "Mon–Sat · Closed Sundays" footer line); the other 19 pages publish no hours in schema or footer. Propagating hours would strengthen local-SEO consistency, **but the specific 08:00-18:00 times are owner marketing info I can't verify** — won't spread an unverified time-claim across 19 pages (never-fabricate). **⚑ OWNER: confirm your hours and I'll propagate `openingHours` (+ footer hours line) to all pages.** No change made this fire. Everything else remains verified-clean (Passes 16–55). Not merged to main.

- **Pass 57** — Created **MORNING_REVIEW.md** — a one-screen, decision-ready digest of the whole overnight run (both repos) so Clifton doesn't have to read 56 passes: what's done & staged, what needs his decision (merge, change-order, g.page), what needs his info (reviews, hours, favicon, photos, env vars), the 19-page redesign-rollout offer, and the InfraNodus-blocked 3D boot screen. Doc-only, consolidates existing NIGHT_LOG detail (no new claims). Directly serves the "stage everything for morning review" instruction. Not merged to main.

- **Pass 58** — InfraNodus finally held a connection → ran the real brain graph + rebuilt the boot screen (Clifton's ask). Captured the Klyfton brain knowledge graph (**150 concepts, 513 links, 11 topical clusters, modularity 0.65 "diversified/high"**) → `mgsf-field-os/public/brain-graph.json` + `brain-graph.js`. **Replaced the gear-machine boot loader with a genuinely 3D force-directed render of THAT real graph**: lit-sphere nodes sized by degree, depth-shaded edges, 11 cluster "regions" labeled at their centroids (Cost Doctrine, Spray System, Engineering/SEER, Action Approval, Revenue Connection, …), slow 3D rotation. Verified headless: graph loads (150/513/11), canvas draws in color, setLayout works, 0 real JS errors. Findings → `mgsf-field-os/KLYFTON_GAP_INSIGHTS.md`: 3 structural gaps (Spray System↔Action Approval, ↔Credential Binding, Guarantee↔Credential) = the technical layer is under-wired to the action/guardrail layers ("good rooms, missing hallways") — matches the manual analysis; owner-gated to fix. Not merged to main.

- **Pass 59** — Built roadmap item #1 (GraphRAG) from Clifton's "make the brain more like you" list. `api/brain-graph-retrieve.js` + `api/brain-graph-data.js`: a **keyless, deterministic retrieval engine** over the real 150-concept brain graph — matches a question to graph concepts (domain alias map + 1-hop edge expansion), scores the 11 clusters, returns which knowledge blocks to load (always keeping voice + DOCTRINE + COMPETITIVE_EDGE). Endpoint `/api/brain-retrieve`. Tested: foam/ROI→Spray/Cost/Engineering, "too cold to spray"→Spray+Safety, SDVOSB→Procurement/FEDERAL, proposal→Action Approval. All 46 api/*.js parse. **Did NOT wire into the live klyfton.js brain** — flipping block-selection changes tuned output + tokens, so staged as a shadow-first, owner-reviewed step. `BRAIN_ROADMAP.md` sequences #2 live-data grounding (needs keys), #3 mesh+proactive (bigger), + eval/vision/voice with honest owner-gated flags. Not merged to main.

- **Pass 60** — Built roadmap item #5 (self-check/eval harness). `mgsf-field-os/tests/calc-invariants.js` (`node tests/calc-invariants.js`, **40/40 passing**): asserts every estimator calculator's internal math identities + monotonicity — foam board-feet & sets=ceil, coating gallons linear, job-cost buildup & realized-GM=target, ROI payback/horizon/net, roof sloped-area geometry, BPI ACH50=cfm×60/vol, dew-point ≤ air temp. **No hardcoded doctrine prices** (catches math regressions, not doctrine — those stay in mgsf-core); exits non-zero on drift; keyless. This is the "brain catches its own drift" trait. Fixed one harness bug (negatives are valid for dew-point temps/ROI net). BRAIN_ROADMAP updated. Not merged to main.

- **Pass 61** — Locked the new GraphRAG engine with a regression suite. `mgsf-field-os/tests/brain-retrieve.js` (`node tests/brain-retrieve.js`, **19/19 passing**): asserts identity+guardrails (base_voice/DOCTRINE/COMPETITIVE_EDGE) are ALWAYS included on every query incl. nonsense/empty; domain routing invariants (foam→FOAM_SPECS+ROI, margin→ACCOUNTING+DOCTRINE, proposal→ACTIONS, SDVOSB→FEDERAL, cold-spray→FOAM_SPECS, equipment→EQUIPMENT); determinism (identical output on repeat); cluster ranking desc; tokenizer stopword handling. Guards api/brain-graph-retrieve.js against future drift before it's ever wired live. Keyless, no brain change. Not merged to main.

- **Pass 62** — WIRED GraphRAG live into the brain (Clifton: "don't stage, just do it"). `assembleBrainBlocks(userText)` now drives BOTH klyfton.js prompt builders (runMind + buildSynthSys): always keeps identity/DOCTRINE/operating-principles/ACTIONS/EXPERT_LIBRARY, selects the heavy domain blocks per question via the graph retrieval. Safe-by-construction: trivial/empty/error → full brain (never worse). Verified: trivial→full 77.7k, "too cold to spray?"→trimmed 54k but keeps FOAM_SPECS+doctrine+voice, broad queries→full. Tests: brain-assembly 28/28, brain-retrieve 19/19, calc-invariants 40/40 all green; klyfton.js loads clean. **NOT merged** — field-os deploy (merge to main) is Clifton's explicit call. Not merged to main.

- **Pass 63** — Built roadmap #2 (live-data grounding), ready for Clifton's HubSpot key. `mgsf-field-os/api/brain-context.js`: reads the app's own synced pipeline from Vercel KV (mgsf:leads/jobs/estimates) + optional HubSpot recent-contacts, and turns it into a one-line "SITUATION (live pipeline)" the brain can ground on (open/cold leads, active jobs $, unsold estimates $). Pure summarize() core + gated gather(); 2.5s fetch timeouts so it can't stall an answer; endpoint /api/brain-context. **Gated & non-fabricating** — no KV/HubSpot → configured:false, empty context (brain adds nothing); reports only real counts. Tests: brain-context 13/13 (+ all api parse). NOT yet wired into klyfton.js hot path — that's a one-liner + live smoke-test once the key/KV is set. Not merged to main.

- **Pass 64** — Wired live-data grounding into the brain (Clifton set HUBSPOT_TOKEN in Vercel, confirmed working). `brainContext.gather()` now folds a real "SITUATION (live pipeline)" into klyfton.js's ctx (both prompt builders) — best-effort, 2.5s timeout, and only fires when the question is about the pipeline (leads/jobs/estimates/money regex) so technical questions don't hit the CRM. All 4 test suites green (calc/retrieve/assembly/context); klyfton.js loads clean. Goes live on field-os→main merge; then smoke-test /api/brain-context for configured:true + real counts. Not merged to main.

- **Pass 65** — Unified test gate. `mgsf-field-os/tests/run-all.js` (`node tests/run-all.js`) runs all 4 brain/estimator suites in one shot and exits non-zero if any fail — usable as a pre-commit/pre-deploy gate. Result: **4 suites, 100 checks, all green** (calc-invariants 40, brain-retrieve 19, brain-assembly 28, brain-context 13). Capstones the session's regression net (GraphRAG + live-data grounding + estimator math) so drift is caught with one command. Keyless, no npm. Not merged to main.

- **Pass 66** — DEPLOYED field-os to main (Clifton's go) + smoke-tested live. Fast-forward-merged the branch (90 commits) → main; Vercel prod deploy READY (commit ad06190). Now LIVE: 3D brain-graph boot screen, GraphRAG block-selection, live-data grounding, warranty button, + all prior staged subsystems; crons now on schedule. **Smoke test /api/brain-context (live) = `{ok:true, configured:true, source:"kv", openLeads:12, coldLeads:11}`** — grounding works on real KV pipeline data. Surfaced a real insight: 11 of 12 leads are cold >7d (follow-up money leaking). Flagged: read endpoints return aggregate data unauthenticated (existing posture — consider CREW_CODE gate). Marketing NOT deployed (separate repo, still has placeholder reviews).

- **Pass 67** — Gated the read endpoints + redeployed (Clifton's go). Added a CREW_CODE gate to `/api/command-center` + `/api/brain-context` (401 crew_code_required when CREW_CODE is set and the code is missing/wrong); updated the app's dashboard fetch to send `?code=klyftonCode()` so the app isn't locked out. Merged to main (49a3d09), Vercel prod deploy READY, all tests green. **Live smoke test: endpoint still returned data with no code → CREW_CODE is NOT set in Vercel, so the gate (and the identical gate on Klyfton AI) is dormant.** ACTION for Clifton: set `CREW_CODE` in Vercel to actually lock the endpoints + the AI to the crew. Gate logic unit-verified (open when unset; 401 when set+wrong; 200 when set+right). Marketing untouched.

- **Pass 68** — (interactive, "do everything while I sleep") **Redesign PROOF page: converted spf-roofing.html to the new `css/site.css` light/navy system** (matches the redesigned homepage) — util bar, logo header + nav, full-bleed hero + scrim, trust strip, card grids, chip service-area, FAQ, Hearth finance block, 3-col footer. **Preserved verbatim:** full `<head>`, all 3 JSON-LD blocks (RoofingContractor+FAQPage+Breadcrumb), FAQ↔schema in sync (4/4), meta desc (146), the g.page review link (untouched). Render-verified at 1280px: stylesheet applies, 0 page errors, no horizontal overflow. **Staged only — NOT deployed** (marketing merge stays gated on Clifton: placeholder reviews + his approval of this look before rolling the other 18 pages). This is the "page 1" to approve. Also this session: deployed field-os to main + smoke-tested (Pass 66), gated read endpoints (67), prod health verified (0 real errors; url.parse warning is Vercel-internal, not ours), CREW_CODE added to GO_LIVE, overnight schedule set.

- **Pass 69** — Schema-consistency fix on the highest-value page. index.html was the **only** content page (20 checked) missing `geo` + `priceRange` in its GeneralContractor schema — the other 19 all carry them. Added the **exact real values already published site-wide** (`GeoCoordinates` 47.105/-104.712 = the Glendive HQ, `priceRange:"$$"`) to index's JSON-LD — pure consistency propagation of the same business's public data, no fabrication. Helps the home page's entity/local signals match the service+city pages Google already sees. Verified: index 2/2 JSON-LD valid, **full site 59/59 blocks valid**. **Flagged (owner-gated, not added):** `sameAs` is absent on all pages — adding it needs Clifton's real, verified social/GBP profile URLs (won't fabricate; g.page link stays untouched). No visual/content change. Not merged to main.

- **Pass 70** — **MAJOR FIX (Clifton flagged): whole site pointed to the WRONG domain.** Every canonical, `og:url`, `twitter:image`, JSON-LD `url`/`image`, sitemap `<loc>`, and the robots.txt `Sitemap:` line used `www.machinegunsprayfoam.net` — but Clifton confirmed the real site is **`www.machinegunsprayfoam.info`** (the app is also on `.info`). Wrong canonicals tell Google to credit a domain you're not using → can bury the real site. GoDaddy availability check: `.info/.net/.com/.org` all registered (consistent with "lots of domains"), `.us` open. Both live domains 403 to automated fetch (normal bot-protection) so couldn't machine-verify which serves the site — went on Clifton's explicit statement. **Swapped all 202 refs `https://www.machinegunsprayfoam.net` → `https://www.machinegunsprayfoam.info` across 22 files** (https + www preserved). Verified: 0 `.net` left, 202 `.info`, **59/59 JSON-LD valid**, sitemap 20/20 on `.info`, robots.txt updated, **g.page review link untouched** (different host). Staged, NOT merged. **⚑ OWNER TODO (registrar/Vercel, not code): point `.net`/`.com`/`.org` to 301-redirect to `.info` so old links + link-equity funnel to the real domain; confirm `.info` is the Vercel primary domain.**

- **Pass 71** — Open Graph completeness: added `og:site_name` ("Machine Gun Spray Foam & Concrete Lifting") + `og:locale` (`en_US`) to all **20 content pages** (were 0/20), inserted right after each page's `og:url`. When a page is shared on Facebook/iMessage/LinkedIn, the card now shows the business name as the source and the correct locale — a small but standard social-preview polish. No fabrication (real brand name already used site-wide), no visible page change. Verified: 20/20 have exactly one of each, **59/59 JSON-LD still valid**. Also confirmed this fire (clean, no change needed): internal links are all extensionless (match `cleanUrls`), and all 38 BreadcrumbList item URLs resolve to real pages after the domain swap. Not merged to main.

- **Pass 72** — (interactive, Clifton's decision) **Reverted the Pass-70 domain swap back to `.net`.** New plan: leave the ProFoam site live on `.info`, and serve THIS repo on `www.machinegunsprayfoam.net` as an improved copy. So this repo's canonicals/og:url/sitemap/schema must point to `.net` (its own domain) — pointing them at `.info` (Pass 70) would have told Google the `.net` pages are duplicates of ProFoam's site and suppressed them. Swapped all 202 refs `.info` → `.net` across 22 files; verified 0 `.info` left, 59/59 JSON-LD valid, canonical/sitemap/robots on `.net`, g.page link untouched. **⚑ NOTE:** two near-identical live sites (.info ProFoam + .net this repo) = duplicate-content risk that splits Google authority; long-term one should 301 to the other, but per Clifton both stay for now. **Open thread:** make `.net` a better copy of the `.info` site — need to see the `.info` site's content (Clifton to share screenshots; no image received yet).

- **Pass 73** — (interactive, Clifton) **Closed parity gaps vs the .info site, using real assets from Clifton's Google Drive.** (1) **Email** — replaced all Gmail refs with the branded `clifton@machinegunsprayfoam.info` (index + spf-roofing, 4 refs → 0 Gmail). (2) **New `/coatings` page** — standalone Protective & Roof Coatings service page (silicone/acrylic, metal/flat/foam), the one `.info` service my site lacked; new-theme (css/site.css), full head/SEO, RoofingContractor+FAQPage(4/4)+Breadcrumb schema. (3) **New `/financing` page** — real **Hearth** lead-capture widget (`api.gethearth.com/lead-capture-widget/a49d1406-…`) + apply link from Clifton's site-assessment doc; $0-down/soft-pull disclosures verbatim, GeneralContractor+FAQ+Breadcrumb. (4) **New `/privacy` page** — adapted from Clifton's Drive draft (`09_Website_Privacy_Policy`), real NAP filled in, set **noindex** pending attorney review (per the draft's own note). Wired into sitemap (coatings+financing added, privacy excluded), homepage (Coatings in specialties, "See Financing Options" in the finance block), and cross-linked footers/nav. Verified: **24 pages, 65 JSON-LD blocks, 0 invalid**; FAQ schema↔visible 4/4 on both new pages; g.page review link untouched; no fabricated numbers/guarantees. **⚑ OWNER:** privacy policy needs your attorney's sign-off before it goes public. Not merged to main.

- **Pass 74** — (overnight) **Internal linking: de-orphaned the new `/coatings` page from its twin, `spf-roofing.html`.** The roofing page is titled "SPF Roofing & Coatings" and has a "Coatings that make it last" section, but didn't link to the dedicated `/coatings` page (Pass 73). Added two contextual links on that one page: a **"Coatings" nav item** (after Insulation) + an **in-copy link** in the Coatings section ("see our protective & roof coatings for metal, flat and foam roofs"). Single file, style preserved, **g.page review link untouched**, 65/65 JSON-LD valid, target verified to exist. **Flagged for a later fire / owner review:** the ~18 older dark-theme pages use a different footer and still don't link to `/coatings` or `/financing` — a broader footer-consistency rollout is worth doing but is too large/heterogeneous for one safe autonomous fire. Not merged to main.

- **Pass 75** — (interactive, Clifton) **Found + used the 100 `.info` screenshots** he uploaded to Drive (loose `IMG_0050`+`0054`–`0152`, all 2026-07-27) — confirmed they're a full walk-through of the live machinegunsprayfoam.info site (the content I'd flagged missing since Pass 72). Decoded/viewed a spread, **mapped the whole `.info` site**, and saved `INFO_SITE_MAP.md` (inventory + .net gap backlog + the "no mold claim" constraint). Then built the two biggest conversion gaps, both on the css/site.css theme with **only verified facts**: **`/about`** (Cliff Behner USMC-veteran story, VOSB/SDVOSB, BPI/ProFoam, MT/WY/ND/SD, both phones, hours — no fabricated stats, no mold claim) and **`/contact`** (full NAP + hours + Maps link + the **exact working SMS quote form** reused from index's `sendQuote()`). Wired both into sitemap + homepage nav + cross-linked footers (new Company column: About/Contact/Financing/Privacy). Verified: **26 pages, 69 JSON-LD blocks, 0 invalid**; canonical/robots/breadcrumb on both; no gmail; correct phone. **Remaining `.info` gaps** (Types of Spray Foam, Attic, Ice Dam, Building Envelope, Strengthens Walls, Rebates, Photo Gallery, T&C) tracked in INFO_SITE_MAP.md for next passes — exact copy is in the un-viewed screenshots. Not merged to main.

- **Pass 76** — (overnight) **Internal linking: de-orphaned `/about` + `/contact` from the new-theme service pages.** After Pass 75 added those pages, `coatings`, `financing`, and `spf-roofing` had **no path to them at all**. Footer grid is locked to 3 columns (`1.4fr 1fr 1fr`) and the nav has no wrap, so — to avoid any layout break — added a compact links line (`About · Contact [· Financing]`) inside each footer's free-text contact block (styled with existing `.foot .contact a` blue). Positive-only: adds links, removes nothing (Service Area column intact). Verified: 3 files +1 line each, all targets exist, **26 pages / 69 JSON-LD blocks / 0 invalid**, g.page link untouched. Remaining orphan gap: the ~18 older dark-theme pages still don't link to the new pages (different footer) — tracked for a later fire. Not merged to main.

- **Pass 77** — (overnight) **SEO QA: trimmed the two over-length meta descriptions.** A meta/OG sweep of all 25 indexable pages found everything consistent except `about.html` and `contact.html` (both new in Pass 75) whose descriptions rendered at ~185/~189 chars — past Google's ~160 SERP cutoff, so they truncated. Rewrote both to ~159/161 chars keeping the primary keywords (veteran-owned, Glendive MT, the four states, services) + phone. Meta-only, no visible/content change, no fabrication. Verified new lengths + JSON-LD still valid (4/4 on the two pages). Also confirmed clean this fire: all 25 pages have title/desc/og:image/og:title/twitter (only `privacy.html` lacks OG, intentional — it's noindex). Not merged to main.

- **Pass 78** — (overnight) **Strengthened the concrete-lifting page with 2 high-intent FAQ entries.** First confirmed sitemap is fully consistent (24 URLs ↔ 24 indexable pages, 0 missing/stale — no fix needed there). Then added to `concrete.html`: **"Is lifting better than replacing the slab?"** (reuses existing slab, faster/less disruptive than tear-out & repour — hedged with "we'll tell you straight when replacement is the better call"; **no dollar figure, no savings guarantee**) and **"Will the foam add weight and make sinking worse?"** (polyurethane is lightweight vs old mudjacking grout). Added to BOTH the visible FAQ and the FAQPage schema — sync now **7/7**; JSON-LD 3/3 valid. Truthful method characterizations only, no fabricated numbers. Not merged to main.

- **Pass 79** — (overnight) **Added FAQPage schema to the homepage (highest-value page).** A per-page FAQ audit found two mismatches: `index.html` had a **visible 4-item FAQ but no FAQPage schema** (leaving rich-result eligibility on the table), and `spray-foam-spec.html` has the reverse (schema FAQ, no visible FAQ). Fixed the homepage: added a FAQPage JSON-LD block that **mirrors the 4 visible Q&As verbatim** (cold-weather spraying, open vs closed cell, financing, service area) — no new claims. Verified: schema text is an **exact match** to the visible `<summary>` text (Google's FAQ-content-must-be-visible rule), index 3/3 JSON-LD valid, full site **70 blocks / 0 invalid**. **⚑ Flagged for next fire:** `spray-foam-spec.html` has FAQPage schema with **no matching visible FAQ** — a structured-data policy mismatch; fix by adding the visible FAQ or removing the schema. Not merged to main.

- **Pass 80** — (overnight) **Fixed 18 stale "Financing" footer links + corrected a false flag.** First, the Pass-79 flag on `spray-foam-spec.html` was a **false alarm**: its FAQ *is* visible (as `<h3>`/`<p>`, not `<details>` — my grep only counted `<details>`), and all 4 questions match the schema verbatim, so it's Google-compliant — nothing to fix there. Real find instead: **18 pages** (all old dark-theme service/city pages + 404) had a footer nav link labeled "Financing" pointing to `/` (home) instead of the dedicated `/financing` page built in Pass 73 — correct label, wrong destination. Global fix: `<a href="/">Financing</a>` → `<a href="/financing">Financing</a>`. Now **26 correct `/financing` links, 0 stale** (the 2 remaining `#financing` are legit on-page anchors on index/spf-roofing). This also de-orphans `/financing` across the old-theme pages (part of the linking gap flagged in Pass 74/76). Verified: 18 files changed, JSON-LD **70/70 valid**, target exists, g.page untouched. Not merged to main.

- **Pass 81** — (overnight) **De-orphaned `/coatings` across the 18 old-theme pages** (continuing the Pass-80 linking rollout). First ran a full broken-internal-link scan: **0 broken links across 26 files.** Then found `/coatings` (built Pass 73) was linked from only 7 pages — the old dark-theme service/city footers listed Spray Foam · Crawl Space · Roofing · Concrete · Financing but **not Coatings**. Verified `SPF Roofing</a> · ` occurs exactly once per page (footer nav only — body links lack the ` · ` separator), so inserted `<a href="/coatings">Coatings</a>` right after SPF Roofing in each footer nav. Now **25 pages link `/coatings`** (was 7), no page double-linked, footer reads "SPF Roofing · Coatings · Concrete Lifting". Verified: 18 files changed, JSON-LD **70/70 valid**, g.page untouched. Remaining old-theme linking gap: About/Contact still absent from these old footers (different nav row) — a later fire. Not merged to main.

- **Pass 82** — (overnight) **De-orphaned `/about` + `/contact` across the old-theme pages** (completes the Pass-80/81 footer-linking rollout). Both pages (built Pass 75) were reachable from only the 6–7 new-theme pages; the 18 old dark-theme service/city footers had no path to them. Verified `Financing</a></nav>` is a single-occurrence footer-nav-end target per page, then appended `· About · Contact` after Financing in each. Now **24 pages link About and 24 link Contact** (were 7), no page double-linked. Re-ran the broken-link scan: **0 broken across 26 files**; JSON-LD **70/70 valid**; 18 files +1 line each; g.page untouched. Only `404.html` + `privacy.html` (separate Company column, noindex) still omit them — minor, optional later. Old-theme footer linking rollout (Financing→Coatings→About/Contact) now complete. Not merged to main.

- **Pass 83** — (overnight) **Strengthened the flagship `spray-foam-insulation.html` with 2 high-intent FAQ entries.** (Skipped mass-adding FAQ to the 11 city pages on purpose — identical FAQ across them would create duplicate-content, not value.) Added **"Is spray foam safe once it's cured?"** (inert once cured + re-ventilated per manufacturer guidance — no health/mold-elimination claim, no guarantee) and **"Does closed-cell foam add strength to the building?"** (closed-cell rigidity adds racking stiffness — a documented property). Added to BOTH the visible `<details>` FAQ and the FAQPage schema; sync now **6/6 with exact text match**, JSON-LD 3/3 valid. No fabricated numbers, no mold/health claims (grep-verified clean). Not merged to main.

- **Pass 84** — (interactive, "do all you can") **Two-repo autonomous push from the AI market scan.** (A) *field-os:* the scan's #1 gap (speed-to-lead) turned out to be **already built** (`api/missed-call.js`) — so I hardened it: added `tests/missed-call.js` (**24 checks** — draft-only/never-auto-send, Sunday+after-hours tone, SMS bounded, name/phone passthrough, brand-safety), wired it into the gate (`run-all.js` → **5 suites, 124 checks, all green**), and documented the exact Twilio→webhook wiring in `GO_LIVE.md` so Clifton can flip it on. (B) *marketing:* built the top remaining `.info`-parity page — **`/types-of-spray-foam`** (open vs closed cell: density, R-value/inch, moisture, strength, hybrid; new-theme, full SEO + FAQPage 4/4 exact-match + Breadcrumb). Standard industry facts only — **no pricing, no mold/health claim, no guarantees** (grep-clean). Wired into sitemap (25 URLs) + de-orphaned site-wide via footer nav (19 pages link it). Verified: **27 pages / 73 JSON-LD / 0 invalid, 0 broken links.** Also committed `AI_MARKET_SCAN.md` (field-os). Not merged to main.

- **Pass 85** — (interactive, "keep knocking out parity pages") **Built the insulation topic cluster — 4 new `.info`-parity pages:** `/attic-insulation`, `/ice-dam-prevention` (strong cold-climate local content), `/building-envelope` (from .info IMG_0102), `/spray-foam-strengthens-walls`. All new-theme, full SEO + GeneralContractor + FAQPage (4↔4 exact-match) + Breadcrumb. Cross-linked as a cluster (each links siblings + the /spray-foam-insulation pillar + /types-of-spray-foam) and the pillar page now links all of them ("Learn more" row). Added all 4 to sitemap (**29 URLs**). Truthful building-science only — **no pricing, no mold/health claims, no guarantees** (grep-clean). Verified: **31 pages / 85 JSON-LD blocks / 0 invalid, 0 broken links.** Remaining `.info` backlog: Rebates (needs real program data — will not fabricate), Terms, Photo Gallery (needs real job photos). Not merged to main.

- **Pass 86** — (overnight) **Built `/terms` (Terms & Conditions)** — the last no-fabrication-risk page in the `.info` backlog. Standard boilerplate modeled on the privacy page: acceptance, services-are-estimates (quotes non-binding until a signed agreement), IP, third-party/Hearth, website "as is", limitation of liability, **governing law = Montana**, real NAP. Set **noindex,follow** (correctly excluded from sitemap, like privacy) — flag for Clifton: have an attorney review before it's treated as final. Linked beside Privacy in the footer **Company** column across the 8 new-theme pages (no doubles). Verified: **32 pages / 85 JSON-LD / 0 invalid, 0 broken links.** **Backlog now:** only **Rebates** (needs real MT/ND/SD/WY program data — will NOT fabricate) and **Photo/Video Gallery** (needs real job photos hosted; Cloudinary is auth-gated) remain — both blocked on owner input, not on me. Not merged to main.

- **Pass 87** — (overnight) **QA sweep + tied crawl-space into the insulation cluster.** First ran a full canonical/robots/sitemap-consistency audit: **32 pages, 0 issues** (every indexable page has a self-referential `.net` canonical + robots; privacy/terms noindex correctly excluded from sitemap; 0 canonical mismatches) — nothing to fix there. Then, since the `.info` parity backlog is owner-blocked (Rebates=needs real data, Gallery=needs photos), made a bounded internal-linking gain: `crawl-space-encapsulation.html` (old-theme, insulation-family) only reached the new cluster via one footer link, so added a "Related spray foam topics" row linking Spray Foam Insulation · Types · Attic · Ice Dam · Building Envelope. Verified: page JSON-LD 3/0 invalid, **0 broken links**, 1 file +1 line, g.page untouched. Not merged to main.

- **Pass 88** — (overnight) **Finished interlinking the insulation family into the cluster.** After Pass 87 tied in crawl-space, the last two insulation-family pages — `metal-building-insulation` and `pole-barn-insulation` (both old-theme) — still only reached the cluster via one footer link. Added a "Related spray foam topics" row after each page's `.bridge` block: Spray Foam Insulation · Types · Foam-strengthens-walls · Building envelope · + the sibling (pole-barn↔metal). Now the whole insulation topic cluster (pillar + types + attic + ice-dam + building-envelope + strengthens-walls + crawl-space + metal + pole-barn) is fully interlinked. Verified: both pages JSON-LD 3/0 invalid, **0 broken links**, 2 files +1 line each, g.page untouched. Not merged to main.

- **Pass 89** — (overnight) Verified the remaining plumbing is clean (robots.txt correct: Allow-all + sitemap ref on `.net`, no noindex pages in sitemap; `api/klyfton.js` brain prose has no typos/doubled-words — no speculative edits made). Then a genuine content strengthen: added **"How do you maintain an SPF roof?"** to the high-value `spf-roofing` page (low-maintenance: inspect, keep drains clear, recoat the coating at end of life — **no fabricated intervals, no guarantee**). Added to both visible FAQ + FAQPage schema; sync now **5↔5 exact-match**, JSON-LD 3/0 invalid, brand-safe. Not merged to main.

- **Pass 90** — (overnight) **Interlinked the service-area (city) cluster** — the last real internal-linking gap. Confirmed all 10 city pages already funnel to services + `/contact` (no gap there), but they had **zero city-to-city links**. Added a "Nearby service areas" section to each of the 10 city pages linking the **other 9** (script-generated, self-excluded, old-theme red link style) — Glendive/Miles City/Sidney/Billings MT · Williston/Dickinson/Bismarck ND · Rapid City SD · Sheridan/Gillette WY. Strengthens local-SEO topical/geographic signals across the regional page set. Verified: every page has exactly 9 sibling links + 0 self-links; **32 pages / 85 JSON-LD / 0 invalid, 0 broken links**; 10 files. Not merged to main.

- **Pass 91** — (overnight) **FAQ schema-accuracy fix on `spray-foam-spec.html`.** A site-wide FAQ schema↔visible audit (27 FAQ pages) found the concrete/insulation/city clusters already fully interlinked (no gaps) and everything synced — except the spec page's FAQPage **answer text was reworded vs the visible `<h3>/<p>` answers** ("1.5 to 2 inches" vs "1.5–2″", "half-inch" vs "½″", etc.). Google wants structured data to match visible content, so aligned all 4 schema answers to mirror the visible text **verbatim** (questions already matched). No visible/content change, no fabrication. Verified: schema answers 4/4 match visible, spec page JSON-LD 3/0 invalid, **site-wide 85 blocks / 0 invalid**. Site is now fully FAQ-consistent. Not merged to main.

- **Pass 92** — (overnight) **Refreshed `MORNING_REVIEW.md`** — it was ~35 passes stale (stuck at Pass 56, still calling the 3D boot screen "blocked" and citing old budget/CREW_CODE banners). Rewrote it current through Pass 91: the `.net` site is now 32 pages fully built + interlinked + SEO-clean; folded in the AI market scan, the connections audit, and the speed-to-lead hardening; and re-sorted the owner action list by leverage — the "money moves" (authorize Twilio → speed-to-lead live, Cloudinary → gallery, Stripe/PayPal → payments, QuickBooks reconnect) up top, then decisions (merge, .net-vs-.info, g.page), then owner-only info (real reviews, attorney review of Privacy/Terms, Rebates data, hours/favicon/photo-compression), then the old-theme redesign rollout. Doc-only, consolidates existing NIGHT_LOG facts — no new claims. Not merged to main.

- **Pass 93** — (interactive, Clifton: "all of the above") **GovCon scan + Rebates page.** (1) **GovCon (Tango):** NAICS 238310/238160/238190 → 10 active federal bids, **all out-of-region** (GA/MD/AR/NY/NJ/OK/NC/TN); weatherization keyword → 2, both out-of-region + past deadline. **No real in-region (MT/ND/SD/WY) opportunity to pursue** — SAM.gov daily scanner covers the baseline. (2) **Built `/rebates`** from real web research: honestly flags the **federal 25C credit ended 12/31/2025** (not presented as current), links real current programs (NorthWestern Energy MT/SD, Rocky Mountain Power wattsmart WY, MT DEQ, MT Home Energy Rebate, DSIRE, ENERGY STAR Rebate Finder), frames MGSF's role as documentation (invoice/R-value/blower-door), **no fabricated $ amounts, explicit "we don't guarantee any rebate."** Wired into sitemap + footer Company column (10 pages). Verified: **33 pages / 88 JSON-LD / 0 invalid, 0 broken links**, FAQ 4↔4. **Still to do from "all of the above": SEO pull (Ahrefs/Semrush) + old-theme redesign rollout.** Not merged to main.

- **Pass 93b** — (interactive) **SEO pull — partial (paid connectors capped).** Semrush = out of API units; Ahrefs Keywords Explorer = "insufficient plan" — so **no exact search-volume report**. Ahrefs free Domain Rating did work: **`.info` DR 3.1** (small local site), **`.net` DR 0.0** (brand new, no backlinks). Wrote `SEO_FINDINGS.md`: both domains near-zero authority → wins are on-page (already done) + off-page (pick ONE canonical domain/301, Google Business Profile, citations, real reviews, a few backlinks) — none needing a paid tool. Flagged the top-up path for the full keyword report. **Remaining from "all of the above": old-theme redesign rollout (big local job, queued next).** Not merged to main.

- **Pass 94** — (interactive, Clifton) **Drive audit + `.com` domain swap (rebuild step 1).** Found new Drive doc **"MGSF Website Copy — .com Rebuild (21 pages)"** — owner-voiced copy for 9 core + 12 sub-pages (incl. a Foam Roofing sub-cluster + Service Areas page not yet built). Clifton's decisions: **target domain = `.com`**; **keep existing URLs + drop in the new copy** (don't restructure); keep branded `.info` email (not the doc's gmail); leave OWNER-INPUT blanks (license #, insurer, financing terms) empty — never fabricate. **Executed step 1:** swapped all URL refs `.net → .com` across 32 files + sitemap + robots.txt. Verified: 0 `.net` left, email still `.info`, g.page untouched, sitemap 30 URLs on `.com`, robots `Sitemap:` on `.com`, **88 JSON-LD blocks / 0 invalid.** **Remaining rebuild steps (queued):** (2) apply the doc's copy to existing pages; (3) build the missing pages (Foam Roofing sub-cluster: benefits/coatings/cool-roofs/seamless/sustainability/uplift + Service Areas). **⚑ OWNER deploy note:** set `.com` as the Vercel primary domain + DNS. Not merged to main.

- **Pass 95** — (interactive, Clifton delegated the call) **Rebuild step 2 (start): applied deck copy to homepage + built Service Areas hub.** Homepage hero subhead → the deck's keyword-rich, owner-voiced line (kept the "We seal it tight…" slogan H1). Built **`/service-areas`** (the deck adds it; my site only had 10 individual city pages, no hub): new-theme, links all 10 city pages by state + all 4 services, GeneralContractor + Breadcrumb schema. Wired into sitemap (31 URLs) + footer Company column (10 pages). Verified: **34 pages / 90 JSON-LD / 0 invalid, 0 broken links**, all 10 city links present. **Remaining rebuild: Foam Roofing sub-cluster (benefits/coatings/cool-roofs/seamless/sustainability/uplift) + drop deck copy into the other core pages.** OWNER-INPUT blanks (license #, insurer, financing terms) still left empty; `.com` needs to be set as Vercel primary domain. Not merged to main.

- **Pass 96** — (overnight) **Built the Foam Roofing sub-cluster (3 pages) from the .com copy deck.** The deck adds a roofing sub-cluster my site didn't have; created **`/spf-roof-benefits`** ("Why a Spray Foam Roof Beats the Alternatives" — seamless/over-existing/insulates), **`/spf-roof-coatings`** ("Coatings That Keep Your Foam Roof Alive" — UV/moisture shield, walkable, recoatable), **`/cool-roofs`** ("Reflect the Heat, Cut the Bill" — reflective coat eases AC load; **no % savings claim**, matching the deck). All new-theme (`css/site.css`), flat slugs to match my URL convention, RoofingContractor + 3-level BreadcrumbList schema, cross-linked to each other + `/spf-roofing` + `/coatings`. Wired into sitemap (34 URLs) + added a "Roofing topics" links row on the `/spf-roofing` pillar + footer Services columns. Verified: **37 pages, 0 broken links, JSON-LD 0 invalid**, branded `.info` email intact, g.page untouched. **Remaining rebuild:** seamless/sustainability/uplift roofing sub-pages + drop deck copy into the other core pages. Not merged to main.

- **Pass 97** — (interactive, Clifton) **Correction: financing = Hearth, NOT an owner-input blank.** Passes 94/95 wrongly lumped "financing terms" into the OWNER-INPUT-blank list. Clifton confirmed financing is done through **Hearth** — and the site already reflects that everywhere: `financing.html` (Hearth rate-check widget + apply link, $0-down, soft-pull disclosure, "Who provides the financing? → Hearth, MGSF is not the lender") plus the Hearth $0-down block on 21 pages. No page change needed. **The only genuine remaining OWNER-INPUT blanks are license # and insurer** — financing is known/live and must not be blanked or restated. Doc-only correction. Not merged to main.

- **Pass 98** — (interactive, Clifton: "check my Drive for the rest") **Verified real business facts from Drive + resolved the license/insurer blanks.** Confirmed from the actual ACORD COI + records: **General Liability $1M/occ, $2M agg (Midvale Indemnity via Stockman Insurance); Workers' Comp via Montana State Fund; UEI H63EELL3K7Z4; MT LLC in good standing.** Added a "Fully insured crew — $1M general liability + MT workers' comp" credential card to About (no policy #s / EIN on the public site — kept those off deliberately). **License finding:** MT does not license insulation/roofing contractors and no contractor license # exists in Drive (only Clifton's personal MT boiler-engineer cert) — so per Clifton's call, **dropped "Licensed & Insured" → "Fully Insured" site-wide** (chips + vetlines + meta, 34 files) rather than imply a license that doesn't exist. Verified: 96 JSON-LD / 0 invalid, 0 broken links, 0 "licensed" refs left, email intact, g.page untouched. **Flagged to Clifton:** internal dossier has a gmail addr, "418 Cooke St," and a 3rd phone that conflict with the live site (site matches COI/boiler license, left unchanged). Not merged to main.

- **Pass 99** — (interactive, Clifton) **Added the Wyoming (307) line + labeled state numbers; confirmed 418 Cooke St stays off-site.** Clifton clarified: 418 Cooke St = his personal home (correctly NOT on the site — business address stays 2402 N Anderson), and the three routing numbers are MT 406-939-8301 / WY 307-296-0625 / SD 605-349-2884 (all push to the AI receptionist; no ND number yet). The WY 307 number was on **zero** pages. Updated the standard footer contact block site-wide to list all three **labeled by state** (MT/WY/SD), and added a "WY local: 307-296-0625" line to the Sheridan + Gillette pages and "SD local: 605-349-2884" to Rapid City — so a local visitor sees a local number. Main CTAs/hero/util still route to the 406. Verified: 96 JSON-LD / 0 invalid, 0 broken links, only the 3 real numbers in any tel: link. Not merged to main.

- **Pass 100** — (interactive, Clifton: "keep going on the deck copy" + "go with certified and fully insured") **Rolled .com deck copy into the 5 remaining core pages.** Pulled the (today-updated) Drive deck and dropped owner-voiced copy into existing flat-URL pages: **About** (H1 "Veteran-Owned. Owner-Operated. On Every Job." + whole-envelope body), **Insulation pillar** (H1 "Spray Foam Insulation That Pays You Back" + air-leak/diagnostics body), **Concrete** (H1 "Raise Sunken Concrete — No Tear-Out, Same-Day Use" + dime-hole/won't-wash-out body; dropped the soft "half the cost" line from the subhead), **Coatings** (H1 "Protective Coatings Built to Take a Beating" + added a Polyurea system card to back the hero), **Contact** (H1 "Get Your Free Quote" + deck body + added the WY number to the reach-us line). Kept branded `.info` email (not the deck's gmail), all schema/FAQ/links intact. **"Certified & Fully Insured"** stands as the trust line (contractor-license chase closed per Clifton — Talia/ProTax has the reg # if we ever want it). Verified: 96 JSON-LD / 0 invalid, 0 broken links, no gmail on site, g.page untouched. Not merged to main.

- **Pass 101** — (interactive, Clifton: dark tactical, USMC scarlet & gold, "no navy") **Recolored the shared stylesheet `css/site.css` from light/navy to dark tactical.** Rendered the two themes for Clifton, who chose dark tactical scarlet & gold (he's USMC — no navy). Rewrote `css/site.css`: charcoal backgrounds (#0f1720/#0a0e13), white headings, **USMC scarlet #c8102e** CTAs/links + **gold #e8a317** accents (eyebrows, checks, footer links, card labels) — matching the old-theme pages' scarlet/gold so the site converges. Logo (black-on-white JPG) framed as an intentional white chip on the dark header; hero scrims switched blue→near-black; mobile-menu JS background fixed #fff→charcoal on 16 files. **This flips all 18 shared-stylesheet pages** (Home, About, Contact, Coatings, Service Areas, Financing, Rebates, all roofing + insulation-cluster sub-pages) to dark tactical in one file. Verified via Chromium renders + 96 JSON-LD/0 invalid, 0 broken links, no missing images. **Next:** convert the 18 old-theme inline-`<style>` pages (concrete, insulation pillar, spec, 10 city pages, soil/seawall) from light-body to dark to finish the unify. Not merged to main.

- **Pass 102** — (interactive, Clifton) **Converted the 18 old-theme inline-`<style>` pages to dark tactical — site now fully unified.** These pages were already half-dark (dark top bar/hero/footer + scarlet/gold) but had white bodies + light cards. Applied a consistent transform to each: white surfaces → dark panels (#19222e), page bg → charcoal (#0f1720), light section bg → #151e29, light borders → #2a3542, dark body/secondary text → light (#e7edf3 / #9fb0bf), headings → white, scarlet/gold accents kept, `.svc .tile h3` stays scarlet. Left the Hearth financing iframe white (it's the third-party widget). Now all 36 content pages (18 shared-CSS + 18 inline) share one dark tactical scarlet & gold look. Verified via Chromium renders (concrete, insulation, Sheridan city page) + 96 JSON-LD/0 invalid, 0 broken links, no stray white surfaces (only the Hearth iframe + 404). Not merged to main. **Note:** 404.html left as-is (minor). 

- **Pass 103** — (overnight) **Converted the last page — 404.html — to dark tactical.** It was the only page still on the old light style (flagged in Pass 102). Applied the same transform: white surfaces → dark panels (#19222e), page bg → charcoal, light borders → #2a3542, dark text → light, scarlet/gold accents kept. Now **all 37 pages** share one dark tactical USMC scarlet & gold look — zero light pages left. Verified via Chromium render + 96 JSON-LD/0 invalid, 0 broken links, no stray white surfaces. Not merged to main. **Remaining deck work (queued):** 3 roofing sub-pages (Seamless Systems, Sustainability, Uplift Resistance).

- **Pass 104** — (interactive, Clifton uploaded logos) **Swapped in the real dark-background logo on the header.** Clifton provided the black-bg version of the emblem (white + red ring on black) — ideal for the dark theme, replacing the white-chip workaround around the old black-on-white `logo.jpg`. Saved as `img/logo-dark.jpg`, pointed all 18 header logos to it (updated width/height to 1659×948), dropped the `.logo .logoimg` white chip, and set the header background to pure black so the logo's black bg blends seamlessly (no box). Verified via Chromium render + 96 JSON-LD/0 invalid, 0 broken links. (Clifton also has animated logos, but they're a blue-glow video — set aside as it clashes with the no-navy direction.) Not merged to main.

- **Pass 105** — (overnight) **Built the last 3 roofing sub-pages from the deck — roofing sub-cluster complete.** Created `/seamless-spf-roofing` ("Seamless Means No Seams for Water to Find"), `/sustainable-spf-roofing` ("A Roof That Lasts — and Saves — for Decades") and `/wind-uplift-resistance` ("Built to Hold Down in Plains Wind") from the Drive deck copy — flat slugs, dark tactical theme + dark-logo header, RoofingContractor + 3-level BreadcrumbList schema, 3 benefit cards each. Kept the lifespan claim conditional ("can last well over 30 years with proper care") — no guarantee, industry-standard, not fabricated. Wired all 3 into sitemap (37 URLs) and fully interlinked the roofing "Roofing topics" rows across the pillar + all 6 sub-pages. Verified: 40 pages, **102 JSON-LD / 0 invalid, 0 broken links**, each new page has canonical + robots, branded `.info` email + state numbers intact. Not merged to main.

- **Pass 106** — (overnight) **Canonical/robots/sitemap audit (clean) + added FAQ to the new uplift page.** Audited all 40 pages: canonical self-referential everywhere, robots present, the 3 noindex pages (404/privacy/terms) correctly excluded from the 37-URL sitemap — no gaps (404 has no canonical, correct for a noindex error page). Since the audit was clean, strengthened `/wind-uplift-resistance` (had no FAQ) with 2 honest FAQ entries + FAQPage schema — "why roofs blow off in wind" and "is foam good for a windy MT/Dakotas site" — no fabricated numbers, no guarantee. Verified: FAQ schema == visible text exact (2/2), **103 JSON-LD / 0 invalid, 0 broken links**. Not merged to main.

- **Pass 107** — (overnight) **Added FAQ + FAQPage schema to `/seamless-spf-roofing`.** Strengthened the thin new seamless page with 2 honest FAQ entries drawn from its own content — "where most roof leaks start" (seams/fasteners/flashing) and "what happens if a foam roof gets punctured" (fully-bonded, leak shows where the damage is, coating renewable) — no fabricated numbers, no guarantee. Matches the site's `.faq` markup + dark theme. Verified: FAQ schema == visible text exact (2/2), **104 JSON-LD / 0 invalid, 0 broken links**. Not merged to main. (Remaining thin roofing pages without FAQ: spf-roof-benefits, spf-roof-coatings, cool-roofs, sustainable — candidates for future fires.)

- **Pass 108** — (overnight) **Added FAQ + FAQPage schema to `/spf-roof-benefits`.** Two honest FAQ entries from the page's own content — "can you install over my existing roof" (often yes, after prep, skip tear-off) and "does a foam roof add insulation" (yes, insulates while it seals; reflective coat cuts cooling) — no fabricated numbers, no guarantee. Site `.faq` markup + dark theme. Verified: FAQ schema == visible exact (2/2), **105 JSON-LD / 0 invalid, 0 broken links**. Not merged to main. (Remaining thin roofing pages without FAQ: spf-roof-coatings, cool-roofs, sustainable-spf-roofing.)

- **Pass 109** — (overnight) **Added FAQ + FAQPage schema to `/spf-roof-coatings`.** Two honest FAQ entries — "what kind of coating goes on a foam roof" (elastomeric/silicone to mfr spec) and "how often does it need recoating" (kept non-numeric: coating is the wear layer, recoat at end of its service life, interval varies — we inspect and give a straight timeline). No fabricated numbers, no guarantee. Verified: FAQ schema == visible exact (2/2), **106 JSON-LD / 0 invalid, 0 broken links**. Not merged to main. (Remaining thin roofing pages without FAQ: cool-roofs, sustainable-spf-roofing.)

- **Pass 110** — (overnight) **Added FAQ + FAQPage schema to `/cool-roofs`.** Two honest FAQ entries — "what is a cool roof" (reflective coat bounces heat, building/AC stay cooler) and "can I put a cool-roof coating on my foam roof" (yes; reflective top coat protects the foam and reflects heat in one layer). **No % savings claim, no guarantee.** Verified: FAQ schema == visible exact (2/2), no "%" in FAQ, **107 JSON-LD / 0 invalid, 0 broken links**. Not merged to main. (Last thin roofing page without FAQ: sustainable-spf-roofing.)

- **Pass 111** — (overnight) **Added FAQ + FAQPage schema to `/sustainable-spf-roofing` — roofing sub-cluster FAQ set now complete.** Two honest FAQ entries — "what makes a foam roof sustainable" (installs over existing → no landfill, recoat vs tear-off, sealed/reflective eases load) and "do you have to tear off the old roof" (usually not, bonds over most low-slope after prep). No fabricated numbers/guarantee (lifespan left conditional as on-page). **All 6 roofing sub-pages (benefits/coatings/cool-roofs/seamless/sustainable/uplift) now carry FAQPage schema.** Verified: FAQ schema == visible exact (2/2), **108 JSON-LD / 0 invalid, 0 broken links**. Not merged to main.

- **Pass 112** — (overnight) **Meta-description length QA — tightened 14 over-long descriptions to ≤160 chars.** Audit found 14 pages with meta descriptions over ~160 chars (Google truncates there): spf-roof-coatings, types-of-spray-foam, rebates, attic, wind-uplift, cool-roofs, strengthens-walls, ice-dam, sustainable, coatings, building-envelope, financing, seamless, contact. Trimmed each to ≤160 without dropping the service/region/phone or adding any claim (pure shortening). Also confirmed BreadcrumbList coverage is complete (only the homepage lacks one, correct). Verified: 0 descriptions >160, 108 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 113** — (overnight) **Homepage FAQ: +2 high-intent entries and removed a duplicate FAQPage block.** Added "Are you licensed and insured?" (answered with the verified **fully-insured — $1M GL + MT workers' comp** fact, veteran-owned, BPI-certified) and "Do you handle commercial jobs, or just homes?" (both — residential + commercial). While adding, found index.html had **two FAQPage JSON-LD blocks** (a stale 4-Q copy in the head + the visible-matching one) — removed the head duplicate so there's now a single FAQPage (6 Qs) matching the visible FAQ exactly. Also confirmed image alt-text coverage is 100% (79 imgs) and breadcrumbs complete. Verified: 1 FAQPage, schema==visible 6/6, **107 JSON-LD / 0 invalid, 0 broken links**. Not merged to main. (Noted for owner review: 37 title tags exceed ~60 chars — left as-is; mass title rewrites are a judgment call for Clifton.)

- **Pass 114** — (overnight) **Added FAQ + FAQPage schema to `/service-areas`.** Two honest coverage FAQ entries — "how far do you travel" (based in Glendive, work across MT/WY/ND/SD, call to confirm range) and "do you serve rural/ag properties" (yes — shops, barns, pole buildings, ag structures plus in-town homes/commercial). No pricing, no fabrication, no guarantee. Now only About and Contact lack FAQ (appropriately — not FAQ-target pages). Verified: FAQ schema == visible exact (2/2), **108 JSON-LD / 0 invalid, 0 broken links**. Not merged to main.

- **Pass 115** — (overnight) **Internal-linking QA: fixed the one thinly-linked page.** Ran an orphan/inbound-link audit across all 39 indexable pages — **no orphans**, but `spray-foam-spec` (the GC/builder "Spec & QA Standard" page) had only 1 inbound link (homepage). Added a contextual link to it from the insulation pillar's "Learn more" related-links row (its strongest topical parent) — now 2 inbound. Verified: 108 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 116** — (overnight) **Full QA sweep — site clean, idling.** Verified across all pages: OG/Twitter tags complete on every content page, `og:url` == canonical everywhere (0 mismatches), sitemap exact (37 indexable = 37 `<loc>`, none missing, none stale), 108 JSON-LD / 0 invalid, 0 broken links, 0 orphan pages. No bounded, safe, non-fabricating improvement remained this fire, so **idling** per instructions. **Open items are all owner-side** (not autonomous-safe): set `.com` as Vercel primary domain + DNS (go-live), merge branch to main, contractor registration # (Talia/ProTax), ND phone number, real Google reviews, attorney review of Privacy/Terms, real job photos for a gallery. Title tags >60 chars (37 pages) flagged for owner — mass rewrite is a judgment call. Not merged to main.

- **Pass 117** — (overnight) **Checked the Klyfton brain option — clean, idling.** Scanned `mgsf-field-os/api/klyfton.js`: parses (`node -c` OK), no doubled words, no common misspellings in the prose (confirms Pass 89). Marketing site remains fully QA-clean (Pass 116). No bounded, safe, non-fabricating improvement available this fire, so **idling**. All open items remain owner-side (see Pass 116: .com DNS/go-live, merge to main, contractor reg #, ND number, real reviews, attorney review, job photos; plus long title tags flagged for owner judgment). Not merged to main.

- **Pass 118** — (overnight) **H1 QA: gave privacy & terms a proper single H1.** Audit found every page had exactly one `<h1>` except `privacy.html` and `terms.html`, which titled with an `<h2>` and had zero H1 (accessibility/structure gap). Promoted each legal-page title to `<h1>` with inline sizing matching the sec-head style (no global CSS change). Also confirmed all pages have `<html lang>`. Verified: 0 pages with ≠1 H1, 108 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 119** — (overnight) **Refreshed sitemap `<lastmod>` to real per-file git-commit dates.** The lastmod values (2026-07-26/27) were stale after the full theme rebuild + copy/FAQ work across nearly every page. Set each of the 36 URLs' lastmod to its file's actual last git-commit date (2026-07-27/28) — accurate crawl-freshness signal, no fabricated dates. XML still parses, 37 `<loc>` intact, image sitemap blocks untouched. Not merged to main.

- **Pass 120** — (overnight) **LCP preload check clean — idling.** Verified each page's `<link rel="preload" as="image">` matches its actual hero image (0 mismatches, so no wasted preloads). With sitemap lastmod (Pass 119), H1s (118), OG/canonical (116), FAQ, breadcrumbs, alt-text, meta lengths, and internal linking all already clean, there's no bounded, safe, non-fabricating improvement left this fire — **idling**. Open items remain owner-side (.com DNS/go-live, merge to main, contractor reg #, ND number, real reviews, attorney review of Privacy/Terms, job photos; long titles flagged). Not merged to main.

- **Pass 121** — (overnight) **Title/description uniqueness verified — clean, idling.** Checked all pages: 0 duplicate `<title>` tags, 0 duplicate meta descriptions (all unique). This was the last unverified on-page SEO dimension; the site is now fully clean across canonical/robots/sitemap/JSON-LD/FAQ/breadcrumbs/alt/meta/titles/OG/H1/lang/preload/internal-linking. No bounded, safe, non-fabricating improvement remains — **idling**. Open items all owner-side (see Pass 116/120). Not merged to main.

- **Pass 123** — (overnight) **Mixed-content / external-host / viewport check clean — idling.** New dimensions verified: 0 insecure `http://` refs, all external hosts expected only (Hearth, Google Maps, g.page [flagged—untouched], cited rebate sites: NorthWestern/Rocky Mountain Power/MT DEQ/DSIRE/ENERGY STAR/MT HPHC), viewport meta on every page. Combined with prior passes, the site is verified clean on every on-page dimension. No bounded, safe improvement remains — **idling**. Open items all owner-side (.com DNS/go-live, merge to main, contractor reg #, ND number, real reviews, attorney review, job photos; long titles flagged). Not merged to main.

- **Pass 124** — (overnight) **Refreshed the stale `MORNING_REVIEW.md` owner-handoff.** It was ~33 passes stale (through Pass 91: called the site ".net"/32 pages/light-theme, listed the old-theme rollout + rebates as TODO — all long since done). Rewrote it to current reality: .com, 40 pages, dark tactical scarlet & gold + dark logo, full deck + roofing sub-cluster, all SEO/QA clean (108 JSON-LD/0 invalid, 0 broken links), insurance verified, and a crisp current owner punch-list (go-live: .com DNS + merge; money-move logins: Twilio/Cloudinary/Stripe/QuickBooks; info: contractor reg #, ND number, reviews, attorney, photos; decision: long titles). Doc-only, no fabrication — consolidates NIGHT_LOG facts. Not merged to main.

- **Pass 125** — (overnight) **FAQ schema↔visible audit + fixed metal-building page.** Deeper FAQ audit (tag-stripped, entity-decoded) found real schema/visible wording drift on 4 old-theme `<details>` pages (concrete ×1, crawl-space ×3, metal-building ×3, pole-barn ×1) — schema answer text didn't match visible verbatim (Google requirement). (spray-foam-spec was a false alarm — its h3/p FAQ matches; also confirmed inline-link answers like attic are fine.) **Fixed `metal-building-insulation`**: rebuilt its FAQPage from the visible FAQ so all 4 Q&As match exactly. Verified: schema==visible 4/4, 108 JSON-LD / 0 invalid, 0 broken links. **Queued for next fires: concrete, crawl-space-encapsulation, pole-barn-insulation** (same fix). Not merged to main.

- **Pass 126** — (overnight) **Finished the FAQ schema↔visible fixes (queued in Pass 125).** Rebuilt the FAQPage schema from the visible FAQ on the remaining 3 pages — **concrete (7 Q&As), crawl-space-encapsulation (5), pole-barn-insulation (5)** — so schema answer text mirrors the visible text verbatim (rendered). Re-audited site-wide: all FAQPage pages now match (spray-foam-spec confirmed a prior regex false-positive — its h3/p FAQ matches 4/4). Verified: 0 real schema/visible mismatches, 108 JSON-LD / 0 invalid, 0 broken links. FAQ structured data is now fully Google-compliant site-wide. Not merged to main.

- **Pass 127** — (overnight) **Content-safety audit vs the hard rules — clean, idling.** Scanned all pages for banned patterns: every "guarantee" occurrence is a **disclaimer** (e.g., "No guaranteed-savings promises," "we don't guarantee any rebate," Terms "does not guarantee any specific result"); **zero** bare %-savings claims; the only "mold" mentions (crawl-space) explicitly state **"We don't remediate existing mold"** — no elimination claim. No fabricated numbers. Site is fully claim-safe. No bounded improvement remained — **idling**. Open items all owner-side (see MORNING_REVIEW.md). Not merged to main.

- **Pass 128** — (overnight) **Softened concrete's unsubstantiated "about half the cost" claims.** The concrete page still ran a specific numeric cost comparison in 4 spots (meta desc, a badge, the sub-hero line, and the lift-vs-replace comparison card: "About half the cost") — a fabricated cost fraction that violates the "never fabricate numbers" rule and contradicted the page's own OG/Twitter tags, which already say "a fraction of tear-out & replacement." Changed all 4 to the softer, non-numeric "a fraction of the cost" so the whole page is consistent and claim-safe. Verified: concrete meta desc 157 chars (<160), 108 JSON-LD / 0 invalid, 0 broken links, no residual "half the cost" copy site-wide. Not merged to main.

- **Pass 129** — (overnight) **Closed the city-page → hub internal-link gap.** All 10 city pages (`spray-foam-insulation-<city>-<st>`) listed sibling cities but none linked back to the `/service-areas` hub — a hole in the hub-and-spoke structure (weakens crawl paths + link equity to the hub). Added an in-style "See all service areas" link (scarlet, matching the existing "Nearby:" links) into each page's "Nearby service areas" paragraph. Verified: 10/10 now link to `/service-areas`, 108 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 130** — (overnight) **Added an FAQ to the About page (was one of the few indexable pages without one).** 4 Q&As — veteran-owned/SDVOSB, service area, insurance, services — every answer grounded verbatim in facts already on the page (no new claims/numbers): the "$1,000,000 GL + Montana workers' comp" line matches the existing insurance card, service area matches the hero/who block. Used the site's standard `<details>` FAQ markup + a matching FAQPage JSON-LD block. Verified: about FAQ schema==visible 4/4, 109 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 131** — (overnight) **Added an FAQ to the Contact page (the last major indexable page without one).** 3 tight, conversion-relevant Q&As — free quotes, hours (Mon–Sat, closed Sundays), and which state number to call — all grounded verbatim in facts already on the page (trust bar "Free quotes", the hours line, the three listed MT/WY/SD numbers). No new claims/numbers; placed below the quote form so it doesn't dilute the CTA. Standard `<details>` markup + matching FAQPage JSON-LD. Verified: contact FAQ schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 132** — (overnight) **Synced stale sitemap `lastmod` dates to real git commit dates.** Recent passes edited pages (concrete, all 10 city pages, about, contact — plus the crawl-space/pole-barn/metal-building FAQ fixes) but their sitemap `<lastmod>` still showed the old 2026-07-27/28 dates — 14 entries stale, understating freshness to crawlers. Regenerated each `<lastmod>` from `git log -1 --date=short` per file. Verified: 0 remaining stale, sitemap XML well-formed, 37 URLs intact. Not merged to main.

- **Pass 133** — (overnight) **Closed a footer internal-link gap on the concrete page.** Audited pillar↔sub-page and service→/financing linking: all pillar/sub links were reciprocal (good), but `concrete.html` was the only service page with no internal link to the `/financing` page (it links out to the Hearth apply URL and has the widget, but not the internal page — inconsistent with the other 6 service pages). Added a "Financing" link to its footer nav in the existing gold style. Verified: 7/7 service pages now link to `/financing`, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 134** — (overnight) **Closed the concrete page's missing Contact/About links.** Ran a site-wide internal-link matrix (contact / service-areas / financing / about): `concrete.html` was the ONLY indexable page with no `/contact` link at all (and no `/about`) — it relied solely on phone CTAs, a real conversion + crawl-path gap. Added About and Contact to its footer nav in the existing gold style. Verified: every indexable page now links to `/contact`, 110 JSON-LD / 0 invalid, 0 broken links. (Noted for a future fire: several pillar/service pages still don't link the `/service-areas` hub — inconsistent but lower priority.) Not merged to main.

- **Pass 135** — (overnight) **Completed the `/service-areas` hub interlinking (follow-up from Pass 134).** 12 indexable pages didn't link the Service Areas hub (all pillars + old-theme service pages), leaving the hub under-linked. Added a "Service Areas" link uniformly: inserted after the footer "Contact" link on the 10 pages sharing the About·Contact span (coatings, crawl-space, financing, metal-building, pole-barn, seawall, soil, spf-roofing, spray-foam-insulation, spray-foam-spec), an "All service areas →" list item in index.html's Service Area footer column, and a Service Areas link in concrete.html's gold footer nav. Verified: 100% of indexable pages now link `/service-areas`, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 136** — (overnight) **Fixed the insulation sub-cluster's one dead-end page.** Sibling cross-linking audit: `types-of-spray-foam` linked 0/4 of its sibling insulation pages (attic, ice-dam, building-envelope, strengthens-walls) while the others linked 3–4 — a cluster dead-end that leaked crawl depth. Added the same "Related — Spray foam topics" morelinks row the sibling pages use (pillar + all 4 siblings + crawl-space), matching the shared theme classes exactly. Verified: types-of-spray-foam now links 4/4 siblings, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 137** — (overnight) **Re-synced sitemap `lastmod` after the internal-linking passes (133–136).** Those passes edited 12 pages (all pillars + old-theme service pages + types-of-spray-foam) whose `<lastmod>` had gone stale again vs their real git dates. Regenerated each from `git log -1 --date=short`. Also confirmed the concrete/soil/seawall cluster is fully cross-linked (no gap there). Verified: 0 remaining stale, sitemap XML well-formed, 37 URLs. (For Clifton: ideally this sync runs at merge/deploy time so it stops drifting between overnight edits.) Not merged to main.

- **Pass 138** — (overnight) **Full local QA sweep — clean; idling.** Verified every locally-checkable dimension: internal linking complete (pillar↔sub, siblings, /service-areas + /contact + /financing on all indexable pages), sitemap synced/well-formed/37 URLs, 110 JSON-LD / 0 invalid, canonical+robots on all, canonical==og:url site-wide, OG/Twitter present, 0 mixed-content (http://) refs, exactly 1 H1/page, 100% image alt-text, robots.txt references sitemap, vercel.json cleanUrls set, 404 page has full nav. Klyfton brain (`api/klyfton.js`) parses and has no doubled-word/typo defect to safely fix (won't touch claims/numbers). No bounded safe improvement remained — **idling**. Remaining work is all owner-side (see MORNING_REVIEW.md). Not merged to main.

- **Pass 139** — (overnight) **Strengthened cool-roofs.html with a third, honest FAQ (was 2).** Added "Does a cool roof make sense in a cold climate like Montana?" — a real regional objection answered straight in the owner's voice: reflective coatings do most in hot climates so the summer benefit is smaller up here; the bigger regional wins are the foam's year-round insulation + UV protection; we'll tell you if it's worth it. No fabricated numbers, no savings guarantee — builds trust by being candid about limits. Standard `<details>` markup + matching FAQPage entry. Verified: cool-roofs FAQ schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 140** — (overnight) **Strengthened spf-roof-coatings.html with a third FAQ (was 2).** Added "Silicone or acrylic — which roof coating is better?" — a real decision customers face, answered factually and non-numerically (silicone for ponding/UV/flex; acrylic cost-effective + reflective on draining slopes; we match it to your roof). No fabricated numbers, no guarantee; consistent with the site's existing "silicone and acrylic coatings" language. Standard `<details>` + matching FAQPage entry. Verified: schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 141** — (overnight) **Strengthened seamless-spf-roofing.html with a third FAQ (was 2).** Added "Can a spray foam roof go over my existing roof?" — a very common pre-quote question, answered honestly: SPF is lightweight and can usually go over a sound existing metal/flat/low-slope roof after prep (avoids tear-off), but a wet/failing roof must be dealt with first, so we inspect. No fabricated numbers, no guarantee; candid about conditions. Standard `<details>` + matching FAQPage entry. Verified: schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 142** — (overnight) **Strengthened wind-uplift-resistance.html with a third FAQ (was 2).** Added "Does a spray foam roof hold up to hail?" — a real Northern Plains concern, answered honestly: the foam + elastomeric coating flex and absorb smaller hail, but no roof is truly hail-proof; the upside is the coating is renewable so dings can be recoated instead of full replacement, and we inspect after big storms. No fabricated numbers, no guarantee, explicitly avoids a "hail-proof" claim. Standard `<details>` + matching FAQPage entry. Verified: schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 143** — (overnight) **Strengthened spf-roof-benefits.html with a third FAQ (was 2).** Added "Is a spray foam roof heavy — can my building handle the weight?" — a real structural objection, answered honestly: SPF is very lightweight (mostly trapped air) so it adds far less load than a tear-off/re-roof or an added layer; a non-issue for most structures, confirmed on assessment. No fabricated numbers, no guarantee. Standard `<details>` + matching FAQPage entry. Verified: schema==visible 3/3, 110 JSON-LD / 0 invalid, 0 broken links. (Roofing sub-cluster now: cool-roofs, spf-roof-coatings, seamless, wind-uplift, spf-roof-benefits at 3 FAQs; only sustainable-spf-roofing still at 2.) Not merged to main.

- **Pass 144** — (overnight) **Strengthened sustainable-spf-roofing.html with a third FAQ (was 2) — roofing sub-cluster now uniform at 3 FAQs each.** Added "How does a spray foam roof lower a building's energy use?" — air-sealing + insulation + reflective coat mean HVAC works less over the roof's life; explicitly declines to promise a dollar figure ("every building is different") to honor the never-guarantee-savings rule. No fabricated numbers. Standard `<details>` + matching FAQPage entry. Verified: schema==visible 3/3, all 6 roofing sub-pages now 3 FAQs, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 145** — (overnight) **Re-synced sitemap `lastmod` for the 6 roofing sub-pages** edited in the FAQ passes 139–144 (all were showing 2026-07-28, real git date 2026-07-29). Also confirmed FAQ depth is now robust everywhere — insulation cluster + service pages all 4–6, roofing all 3; service-areas' 2 already cover travel-range + rural/ag so a third would overlap or drift into pricing (skipped as padding). Verified: 0 stale, sitemap well-formed, 37 URLs. (Reminder for Clifton: automate this sync at merge/deploy.) Not merged to main.

- **Pass 146** — (overnight) **Site-wide FAQ schema↔visible consistency audit — clean; idling.** Ran a full audit across all 37 FAQPage pages (both `<details>` and spray-foam-spec's `<h3>`+`<p class="lead">` styles). Initial bulk pass flagged 4 on spray-foam-spec, but anchored per-entry comparison proved all 4 are byte-identical (e.g. the "hybrid cavity" answer: 340==340 chars, EQUAL) — the flags were a non-anchored-regex artifact from that page's multiple spec-section `<h3>`s, not real drift. Conclusion: 0 real schema/visible mismatches site-wide; FAQ structured data remains fully Google-compliant. No change needed — **idling.** (Note for future fires: spray-foam-spec's h3/p FAQ format trips naive `<details>`-only matchers; use anchored checks.) Not merged to main.

- **Pass 147** — (overnight) **Added visible breadcrumb navigation to the 6 roofing sub-pages.** They already had BreadcrumbList JSON-LD but no visible trail for users; added a themed `Home › SPF Roofing › [Page]` breadcrumb bar right below the header (current page as non-linked `aria-current`), with a new `.crumbs` rule in css/site.css (charcoal bar, muted links, gold hover) — labels mirror each page's existing JSON-LD exactly. Real UX + crawl-context win, matches dark theme. Verified: 6/6 have the visible crumb, 110 JSON-LD / 0 invalid, 0 broken links. (Future fires could extend the same crumb to the insulation sub-cluster + concrete/soil/seawall.) Not merged to main.

- **Pass 148** — (overnight) **Extended visible breadcrumbs to the 5 insulation sub-pages** (types-of-spray-foam, attic-insulation, ice-dam-prevention, building-envelope, spray-foam-strengthens-walls) — `Home › Spray Foam Insulation › [Page]`, reusing the `.crumbs` style from Pass 147; labels mirror each page's BreadcrumbList JSON-LD exactly. Verified: 5/5 have the visible crumb, 110 JSON-LD / 0 invalid, 0 broken links. (Remaining without visible crumb: concrete + soil/seawall old-theme pages and the 10 city pages — future fires.) Not merged to main.

- **Pass 149** — (overnight) **Added visible breadcrumbs to the 10 city pages + upgraded their BreadcrumbList to 3-level.** They previously had only a 2-level trail (Home › City) and no visible crumb. Inserted the `/service-areas` hub as the middle step in both the JSON-LD (now Home › Service Areas › [descriptive city page]) and a new self-contained dark-styled visible crumb (Home › Service Areas › [City, ST]) — old inline theme, so colors are inline (dark #14181d bar, muted #9fb0bf links, white current) to guarantee contrast. City short labels parsed from each page's JSON (handles "Miles City" correctly). Verified: 10/10 have 3 ListItems + visible crumb + Service Areas in trail, 110 JSON-LD / 0 invalid, 0 broken links. (Remaining without visible crumb: concrete/soil/seawall — future fire.) Not merged to main.

- **Pass 150** — (overnight) **Added visible breadcrumbs to concrete, soil-stabilization, seawall-stabilization** (old inline theme). These are top-level service pages with correct 2-level BreadcrumbList JSON-LD, so added a matching visible 2-level crumb (Home › [Service]) — no JSON-LD change needed — using the same self-contained dark inline style as the city pages (guaranteed contrast). Verified: 3/3 have the crumb, 110 JSON-LD / 0 invalid, 0 broken links. Remaining no-crumb: top-level pillars (spray-foam-insulation, spf-roofing, coatings, service-areas, about, contact, financing, rebates — Home › self adds little) + 4 service sub-pages (crawl-space, metal-building, pole-barn, spray-foam-spec) for a future fire. Not merged to main.

- **Pass 151** — (overnight) **Finished the visible-breadcrumb rollout — added to the last 4 service sub-pages** (crawl-space-encapsulation, metal-building-insulation, pole-barn-insulation, spray-foam-spec; old inline theme). Matching 2-level `Home › [Service]` crumbs mirroring each page's existing BreadcrumbList JSON-LD, same dark inline style. Rollout now complete: every sub-page/service page with a real parent hierarchy has a visible crumb (roofing ×6, insulation ×5, city ×10, concrete/soil/seawall ×3, these ×4). The 8 pages still without one are all top-level pillars/company pages (spray-foam-insulation, spf-roofing, coatings, service-areas, about, contact, financing, rebates) — direct children of Home, where a "Home › self" crumb is noise, so intentionally skipped. Verified: 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 152** — (overnight) **Re-synced sitemap `lastmod` after the breadcrumb passes (147–151).** Most edited pages already matched their git date; 4 entries had drifted and were corrected to real `git log` dates. Verified: 0 remaining stale, sitemap XML well-formed, 37 URLs. (Standing reminder for Clifton: automate this sync at merge/deploy so it stops drifting between overnight edits.) Not merged to main.

- **Pass 153** — (overnight) **Checked for remaining safe wins — none genuine; idling.** Homepage business schema already complete (has openingHours; no verified socials to add without fabricating). Scanned `api/klyfton.js`: parses clean, no TODO/placeholder/typo/doubled-word defects — no safe clarity fix presents itself (won't touch claims/numbers). Marketing site is comprehensively optimized: FAQ depth robust, internal linking + `/service-areas` hub complete, visible breadcrumbs on all 28 sub/service pages, sitemap synced, all SEO/QA dimensions clean. Further micro-edits would be padding, not improvement — **idling.** Remaining work is owner-gated (MORNING_REVIEW.md: go-live/DNS, merge, Twilio/Cloudinary/Stripe, contractor reg #, ND number, reviews, attorney, photos). Not merged to main.

- **Pass 154** — (overnight) **Closed the homepage's missing footer links to Rebates, Privacy & Terms.** index.html's footer had contact/Services/Service-Area columns but no Company/legal links — the homepage (highest-authority page) linked to `/rebates` (an indexable page), `/privacy`, and `/terms` NOWHERE. Added a centered company/legal row before the vetline (About · Contact · Financing · Rebates · Privacy · Terms), inheriting the existing `footer a` color. Verified: homepage now links rebates/privacy/terms, 110 JSON-LD / 0 invalid, 0 broken links. Not merged to main.

- **Pass 155** — (overnight) **Fixed footer legal-link inconsistency: added Terms next to Privacy on 7 pages.** The 6 roofing sub-pages + service-areas had a footer "Company" column linking Privacy Policy but NOT Terms & Conditions (their sibling legal page) — inconsistent with about/contact/rebates footers. Added `<li><a href="/terms">Terms &amp; Conditions</a></li>` right after the Privacy line on each. Verified: 7/7 now link /terms, 110 JSON-LD / 0 invalid, 0 broken links. (Noted: the 10 city pages + concrete/soil/seawall old-theme footers link neither privacy nor terms — a separate footer variant; candidate for a future fire.) Not merged to main.

- **Pass 156** — (overnight) **Added Privacy + Terms footer links to the 10 city pages** (old-theme footer nav previously ended at About · Contact — no legal links). Appended `· Privacy · Terms` after Contact uniformly (plain links matching the row's existing style). Verified: 10/10 now link both /privacy and /terms, 110 JSON-LD / 0 invalid, 0 broken links. (Remaining old-theme footers still missing legal links: concrete, soil-stabilization, seawall-stabilization — their nav uses gold inline styles; next fire.) Not merged to main.

- **Pass 157** — (overnight) **Added Privacy + Terms footer links to concrete, soil-stabilization, seawall-stabilization.** soil/seawall used plain footer-nav links (appended `· Privacy · Terms` after Service Areas); concrete uses gold-inline links (added gold Privacy · Terms before Home). Verified: 3/3 now link both, 110 JSON-LD / 0 invalid, 0 broken links. Remaining without /terms (and /privacy): 8 pages — coatings, financing, spf-roofing, spray-foam-insulation (shared-theme pillars) + crawl-space, metal-building, pole-barn, spray-foam-spec (old-theme) — mixed footer variants, next fire. Not merged to main.

- **Pass 158** — (overnight) **Completed site-wide footer legal links — added Privacy + Terms to the last 8 pages.** Handled three footer variants: plain nav rows (spray-foam-insulation, crawl-space, metal-building, pole-barn, spray-foam-spec), the About·Contact·Financing span (spf-roofing, coatings), and financing.html's span. Now EVERY indexable page links both /privacy and /terms (previously /terms was on only a handful). Verified: 0 pages missing either, 110 JSON-LD / 0 invalid, 0 broken links. Legal-footer consistency is now complete site-wide. Not merged to main.

- **Pass 159** — (overnight) **Re-synced sitemap `lastmod` after the footer legal-link + homepage passes (154–158).** Those touched ~30 pages; 29 `<lastmod>` entries had drifted from their real git dates and were corrected. Verified: 0 remaining stale, sitemap XML well-formed, 37 URLs. (Standing reminder for Clifton: automate this sync at merge/deploy so it stops drifting between overnight edits.) Not merged to main.

- **Pass 160** — (overnight) **Solved the recurring sitemap-drift root cause: added `tools/sync_sitemap.py`.** A standalone, dependency-free script that rewrites each sitemap `<lastmod>` from the file's real `git log` date. Two modes: default (fix in place) and `--check` (report drift, exit 1 — CI-ready). Replaces the manual re-sync I've had to run every few fires (Passes 119/132/137/145/152/159); Clifton can wire `--check` into CI and the apply mode into a pre-deploy step. Tested: `--check` → "in sync (0 drift)" exit 0, apply → no-op, sitemap unchanged + well-formed (37 URLs). New file only — no page changes. Not merged to main.

- **Pass 161** — (overnight) **Refreshed the stale `MORNING_REVIEW.md` owner handoff (was current only through Pass 124, ~36 passes behind).** Updated header to Pass 160, bumped JSON-LD count 108→110, broadened the FAQ/QA line, and added a "Since the last review (125–160)" section summarizing the completed internal-linking (hub/contact/financing site-wide), visible breadcrumbs on all 28 sub/service pages, raised FAQ depth (About/Contact + roofing cluster to 3 each), site-wide Privacy+Terms footers, the concrete claim softening, and the new `tools/sync_sitemap.py`. Doc-only, consolidates NIGHT_LOG facts — no fabrication, punch-list unchanged. Not merged to main.

- **Pass 162** — (overnight) **Added `tools/qa_check.py` — the local QA gate as one CI-ready command.** Consolidates the checks I run every fire: JSON-LD parses, internal links resolve, canonical+robots present (404/privacy/terms exempt), and FAQPage schema == visible text (rendered/tag-stripped/entity-decoded). Dependency-free; exits 1 on any failure. Notably its FAQ matcher uses `<h3>([^<]*)</h3>` so it does NOT trip the long-standing spray-foam-spec false positive (the h3/p FAQ that naive matchers mis-pair — see Pass 146). Tested: full site → "PASS", exit 0, 40 pages / 110 JSON-LD. New file only. Not merged to main.

- **Pass 163** — (overnight) **Documented the maintenance scripts: added `tools/README.md`.** Explains what `qa_check.py` and `sync_sitemap.py` do, how/when to run them (from repo root), their exit codes, and a suggested pre-deploy order (sync sitemap → run QA gate) plus CI wiring via the `--check`/exit-1 modes. Ties off the tooling from Passes 160 & 162 so Clifton can actually use it. Verified both scripts behave as documented (sitemap in sync, QA PASS). Doc + confirmation only. Not merged to main.

- **Pass 164** — (overnight) **Added a favicon link to all 40 pages (was 0 — every tab showed a blank icon + a /favicon.ico 404).** Wired `<link rel="icon" type="image/jpeg" href="/img/logo-dark.jpg">` into every `<head>` so the browser tab shows Clifton's real black-background logo instead of nothing. Uses the existing real asset — no fabrication. Verified with the new QA gate: PASS (40 pages, 110 JSON-LD, links/canonical/FAQ all clean). **For Clifton:** logo-dark.jpg is wide (1659×948); a dedicated square 512×512 PNG + a favicon.ico would render sharper at tab size — supply one and I'll swap it in. Not merged to main.

- **Pass 165** — (overnight) **Re-synced sitemap `lastmod` after the favicon pass — first real use of `tools/sync_sitemap.py`.** The Pass-164 favicon edit re-dated all 40 pages; the tool found 8 `<lastmod>` entries still at 2026-07-29 and corrected them to 2026-07-30, then `--check` confirmed 0 drift. Well-formed, 37 URLs. The script does exactly what the manual re-syncs used to (Passes 119/132/137/145/152/159) in one command — going forward this is a one-liner, not a hand pass. Not merged to main.

- **Pass 166** — (overnight) **Added `apple-touch-icon` to all 40 pages** (companion to Pass 164's favicon; iOS home-screen bookmarks + some share previews use it). Same real asset (`/img/logo-dark.jpg`), inserted right after the `rel="icon"` link in every `<head>`. Verified with QA gate: PASS (40 pages, 110 JSON-LD, all clean). Then ran `tools/sync_sitemap.py` to re-date the touched pages. Icon setup is now complete (favicon + apple-touch-icon site-wide; the square-PNG upgrade note for Clifton still stands). Not merged to main.

- **Pass 167** — (overnight) **End-to-end verification via the committed tools — all green; idling.** `qa_check.py` → PASS (40 pages, 110 JSON-LD, links/canonical/robots/FAQ-schema all clean); `sync_sitemap.py --check` → 0 drift; favicon + apple-touch-icon present on all 40 pages; `api/klyfton.js` parses. The site is comprehensively complete (theme, copy deck, internal linking + hub, breadcrumbs, FAQ depth, legal footers, icons, tooling). No safe improvement remains that wouldn't be padding — **idling.** All open items are owner-gated (MORNING_REVIEW.md: go-live/DNS + merge, Twilio/Cloudinary/Stripe/QuickBooks logins, contractor reg #, ND number, real reviews, attorney review, job photos, square favicon PNG, decision on long title tags). Not merged to main.

- **Pass 168** — (overnight) **Removed a now-stale date claim on the Rebates page.** The utility card said NorthWestern Energy "(insulation rebates offered through mid-2026)" — as of late July 2026 that window is expiring/past and reads as misleading. Changed it to the non-dated "(check their current insulation rebates)," which keeps the link and matches the page's own "programs change constantly — confirm current terms" framing. No fabricated replacement date (can't verify current program status offline). Verified: 0 "mid-2026" refs left, QA gate PASS. Not merged to main.

- **Pass 169** — (overnight) **Site-wide stale-date/year sweep — clean (after Pass 168's fix); idling.** Scanned all pages for copyright years, `2024/2025` mentions, and time-bound phrasing ("through/until 20XX", "this year", "currently"). Findings: no copyright line exists anywhere (nothing to go stale); every `2025` reference is the **accurate** federal 25C credit "ended for projects completed after December 31, 2025" wording (correctly past-tense, defers to a tax pro) — not stale. The one genuinely stale date ("NorthWestern rebates through mid-2026") was already fixed in Pass 168. No change needed — **idling.** Deliberately did NOT add a hardcoded "© 2026" line (it would just become next year's staleness). Not merged to main.

- **Pass 170** — (overnight) **Refreshed the badly-stale `TODO.md` (was dated Pass 93 / 2026-07-27).** It still described a `.net` site, a "light theme" redesign of "19 old-theme pages," a `.net`-vs-`.info` domain decision, favicon and `openingHours`/business-hours as open, and the crawl-space page as MISSING — all long since done or obsolete (site is now unified dark `.com`, 40 pages; crawl-space built; favicon + hours done). Rewrote to current reality: kept only the genuinely-open owner-gated items (Twilio/Cloudinary/Stripe/QuickBooks, .com go-live + merge, contractor reg #, ND #, reviews, attorney, photos, square favicon, title-tag decision) and a truthful "Done & staged" summary. Doc-only, consolidates NIGHT_LOG facts — no fabrication. Not merged to main.

- **Pass 171** — (overnight) **Marked the stale `INFO_SITE_MAP.md` as a completed historical migration tracker.** It was a Pass 75–93 `.info`→`.net` port gap-tracker whose "still missing" backlog (Types/Attic/Ice-Dam/Building-Envelope/Strengthens-Walls/Rebates/Terms) is all built and whose `.net` framing is superseded by `.com`. Rather than rewrite a historical artifact, added a top STATUS banner: migration complete, only the owner-gated Photos/Video Gallery remains, and it points to TODO.md/MORNING_REVIEW.md as the authoritative current status (mold-claim guardrail noted as still standing). Doc-only, no fabrication. Not merged to main.
